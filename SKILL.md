---
name: bid-evaluation-assistant
description: 用于从政府采购云平台获取的投标机会清单中，自动初筛出值得关注的采购项目，并结合公司介绍对招标文件（标书）进行 AI 研判，输出适合/不适合 + 简要理由，初筛结果输出 Excel（清单路径），研判结论整理为 Markdown 研判报告（标书路径）供查看。可选前置：按地市自动采集政府采购公告（浙江政府采购网，主文件即清单）。当用户表达"投标 / 标书 / 研判 / 招标机会 / 开始评估 / 开始研判 / 采集 / 采集公告 / 采集并初筛"等意图时触发。纯对话式交互，无需 widget。
---

# 投标机会评估助手

## 一、触发条件

当用户表达以下任一意图时触发本 Skill：

- 提到"投标""标书""招标""研判""招标机会""评估""参标"等关键词时
- 已放置投标机会清单 Excel 或标书文件后说"开始研判""开始评估"时
- 要求对某个采购项目判断是否适合本公司参标时
- 提到"采集""采集公告""采集并初筛""抓取/爬取采购公告""更新公告"等意图时（进入阶段 A：公告采集，可选前置）
- 明确说"全部重新初筛""重新研判全部项目"时（先重置状态再全量初筛，复评）

## 二、首次使用引导（最先执行）

> 用户第一次打开本项目时，通常不知从何入手。*在进入核心工作流之前，先询问文件所在路径，并主动给出引导。*

1. 本项目**不依赖固定文件夹**：清单、标书、公司资料放在哪里，由用户在对话中告知路径。
2. 若用户尚未告知路径，主动提示用户：

   > 请告诉我以下三个文件夹的路径（直接粘贴 Windows 路径即可）：
   > 1. **清单路径**：存放投标机会清单 Excel（.xlsx）的文件夹（支持多个地市清单一起处理；也可由本技能"采集"自动生成）；
   > 2. **标书路径**：存放下载的招标文件（.docx/.doc/.pdf）的文件夹；
   > 3. **公司资料路径**：存放公司介绍.md、业绩表.xlsx、人员社保表.xlsx 的文件夹。
   > 告诉我后，我会先确认各路径下的文件，再开始研判。

3. 收到路径后，先**回读确认**路径下有哪些文件（清单有哪些 .xlsx、标书有几个、公司资料是否齐全），
   确认无误后**直接进入「步骤 2：确认项目类型范围」**（无需重复确认路径）。
4. **采集为可选前置**：尚无清单时可直接说"采集"，由技能自动生成主文件清单后进入研判；
   已有清单/主文件的用户跳过采集，直接说"开始研判"即可。

> **核心工作流的第一步是「确认项目类型与时间范围」，每次研判都会先问您要筛选的类型与时间窗口，再开始初筛（增量模式下仅对新增批次询问）。**

## 三、核心工作流（按顺序执行）

> 融合后为**一个技能两个阶段**：阶段 A（公告采集，可选前置）→ 阶段 B（增量初筛 + AI 研判）。
> 已有清单/主文件的用户可跳过阶段 A，直接说"开始研判"进入阶段 B（R10）。

### 阶段 A：公告采集（可选前置）

> 当用户表达"采集""采集公告""采集并初筛""抓取/爬取采购公告""更新公告"等意图时进入本阶段。
> 采集产物：主文件 `{清单路径}/{地市}政府采购公告.xlsx`（12 列：10 数据列 + 初筛状态/研判状态），
> 主文件即阶段 B 的清单输入，无数据搬运断点（方案 2.2）。

> **多 Sheet 主文件增量采集（推荐，R 增量口径）**：清单为 `政府采购公告.xlsx`（不同地市各一个 Sheet）时，
> 用 `scripts/collect_round.py` 一步完成增量采集：**按各城市 Sheet 最后一条公告时间起算**（含当日，下午新发布公告可补到；
> Sheet 无数据默认最近 3 天；最多回溯 10 天）、按 2 天一批自动拆批采集、自动合并入对应城市 Sheet（URL 去重、新增行标「新增待初筛」）。
> 固定窗口采集会重复抓取已入库数据，**不要**用 `fetch_zfcg.py` 手工指定重复范围；今晚 18:00 后的"补采"同样直接重跑本命令即可（幂等）。
>
> ```bash
> python scripts/collect_round.py --workspace "清单路径" --cities "丽水市,杭州市"   # --cities 省略=全部 11 地市
> ```

1. **确认地市**：每次采集询问一次，不落盘记忆。列出浙江 11 地市（杭州/宁波/温州/嘉兴/湖州/绍兴/金华/衢州/舟山/台州/丽水，默认浙江内地市），用户回复名称（如"丽水市"）；地市配置为技能内置（references/districts.json），用户无需修改。
2. **确认清单路径**（=采集工作区，复用三路径告知模式）：主文件与批次文件均放该文件夹；三个路径（清单/标书/公司资料）可相同可不同，由用户每次告知，不落盘记忆。
3. **自动分析缺失日期**（主文件不存在 → 提示"主文件不存在，将采集最近 3 天数据"并跳过本步）：

```bash
python scripts/analyze_main.py --city 丽水市 --workspace "清单路径"
```

4. **自动采集**（自动模式：脚本内部重新分析缺失日期，口径与上一步 `analysis_result.json` 一致；补采 >2 天按 2 天一批拆分执行）：

```bash
python scripts/fetch_zfcg.py --city 丽水市 --workspace "清单路径"
```

   - 补采 >2 天时告知："本次需补采 N 天，预计约 X 分钟，将分 K 批进行，请保持网络畅通"；
   - API 失败/超时/限流：脚本既有 try/continue 保留，AI 对该失败日期自动重试一次，仍失败给出中文提示（不阻塞其余日期）；
   - 当日 18:00 前采集今天数据时提示"下午公告可能未发布完，建议 18:00 后补采"；
   - 性能约束沿用采集器既有：单日 <50 条 ~180s 超时、多日 >100 条 600s 超时、单次 ≤200 条、list 页间 200ms、detail 间 300ms。

5. **合并入主文件**：

```bash
python scripts/merge_to_main.py --city 丽水市 --workspace "清单路径"
```

   - URL 去重、公告时间倒序；新增行自动写状态列（初筛状态=新增待初筛、研判状态=空）；
   - 批次文件（{地市}_采购公告_YYYY-MM-DD.xlsx）合并后自动删除，不残留；
   - 主文件不存在时脚本自动创建空主文件（12 列表头），零代码首次运行不中断。

6. **汇报**："本次新增 N 条公告（跳过重复 M 条），主文件共 T 条"，并告知主文件位置：`清单路径/{地市}政府采购公告.xlsx`。
7. 若用户说"采集并初筛"→ 继续阶段 B；若仅"采集"→ 结束，提示可说"开始研判"继续。

### 阶段 B：增量初筛与研判（核心工作流）

> **增量机制（方案 4.2，R6/R7/R8）**：以「地址链接URL」为唯一键；「初筛状态≠已初筛」（新增待初筛或空）= 新增，进入本次初筛批次；「初筛状态=已初筛」自动跳过初筛；「研判状态=已研判」自动跳过研判。**默认任何"开始研判/采集并初筛"只处理新增，不重置**；复评需用户明确说"全部重新初筛""重新研判全部项目"。
>
> 状态载体双轨：采集主文件（自带 初筛状态/研判状态 列）与用户自备清单（状态写 清单路径/增量状态.json，**不改写用户清单文件本身**），统一由 `scripts/status_tool.py` 读写（方案 4.3）。

本 Skill 使用 **Skill + 本地脚本混合** 形态：文件解析与初筛交给 `scripts/` 下的 Python 脚本，AI 研判由本对话直接执行。

### 步骤 1：确认三个路径与文件就绪

- **复用首次引导已提供的路径，不重复询问**；仅当用户本次会话尚未提供路径时，询问并记录三个路径（**每次研判都要问，不落盘记忆**）：
  - **清单路径**：放置投标机会清单 .xlsx 的文件夹（支持多个清单自动合并，各文件表头行自动探测）；
  - **标书路径**：放置标书 .docx/.doc（老格式，Word 自动转换；本机无 Word/未装 pywin32 时提示"请用 Word 另存为 .docx 后重试"）或误放入 .pdf 招标文件的文件夹；
  - **公司资料路径**：放置 公司介绍.md、业绩表.xlsx、人员社保表.xlsx 的文件夹。
- **标书可选自动下载（新能力，需用户明确要求）**：
  - 标书仍可人工下载放入；也可由 `scripts/fetch_bid_docs.py` 自动从政采云下载「待研判清单」各项目的招标文件：
    登录（一次性）→ `python scripts/browser_login.py`（弹出浏览器窗口，**用户本人在窗口内登录**，脚本不接触账号密码；
    登录态加密存本机 `%LOCALAPPDATA%/投标评估助手/浏览器档案`，过期重登一次即可）→
    `python scripts/fetch_bid_docs.py --out-dir "标书路径"`（列表搜索 → 弹层下载 → 签名直链落盘；
    未获取项目自动填表提交申请，联系人信息存 `%LOCALAPPDATA%/投标评估助手/申请信息.json`）。
  - 每下载一个文件自动写入 `标书路径/标书映射.json`（文件名→项目名），下次运行流水线直接复用，无需再人工确认匹配；
  - 依赖：`pip install -r requirements_download.txt`（浏览器用系统 Edge，零下载；chromium 需 `python -m playwright install chromium`）。
- **回读确认**：向用户展示三个路径下找到的文件清单（清单 .xlsx 文件名、标书数量、公司资料是否齐全），确认无误后继续。
- 若路径不存在或关键文件缺失，用中文提示用户后再继续，不强行执行。

### 步骤 2：确认项目类型范围（初筛前必做；增量模式下仅对新增批次确认，只问一次）

**每次研判都要重新确认；增量模式（--incremental）下仅当存在新增记录时询问，只问一次（R11），无新增时跳过本步。**

1. 20 大类 + 52 细分子类模板**固定在 Skill 内部**（本列表 + `scripts/excel_tool.py` 内置常量，不随业务路径；
   已消除"其他"兜底类，五级匹配保证每个项目都有明确类型归属），**先让用户选大类（可多选）**，再对每个选中的大类**让用户选子类（可多选，默认全部子类）**：

```
请选择本次要筛选的项目类型大类（回复编号或名称，可多选）：
 1. 办公设备与家具      2. 家电机电与通用设备   3. 教学装备
 4. 教育培训服务        5. 医疗设备与医用耗材   6. 车辆船艇与运输
 7. 建筑工程与装修改造  8. 市政与园林水利工程   9. 电力电气与新能源工程
10. 信息化系统与集成   11. 网络安全与数据安全  12. 信息技术服务
13. 后勤物业与餐饮     14. 医疗健康服务        15. 安全消防与应急
16. 专业服务           17. 文化宣传与文体活动  18. 农林牧渔与生态环保
19. 民政福利与养老     20. 电子卖场类

各大类子类一览（选择大类后展示，子类可多选；不选=该大类全部子类）：
 1. 办公设备与家具：计算机与显示设备 / 打印复印与耗材 / 办公家具与用品 / 宿舍与生活家具 / 音视频会议设备
 2. 家电机电与通用设备：空调暖通设备 / 家用电器 / 电梯与机电设备 / 厨房设备 / 劳保与通用物资
 3. 教学装备：教学仪器与实验设备 / 实训与教具设备 / 教育信息化设备 / 体育器材与健身设施 / 校园后勤设施
 4. 教育培训服务：培训与课程服务 / 人才与人力资源服务 / 疗休养与职工福利
 5. 医疗设备与医用耗材：影像诊断设备 / 治疗急救设备 / 检验与实验室设备 / 药品试剂与医用耗材
 6. 车辆船艇与运输：公务与专用车辆 / 船艇采购 / 运输物流与燃油
 7. 建筑工程与装修改造：土建施工 / 改造修缮工程 / 装修装饰与布展 / 拆除搬迁与场地
 8. 市政与园林水利工程：市政道路管网 / 园林绿化景观 / 水利防洪防汛
 9. 电力电气与新能源工程：电力配电工程 / 光伏与新能源 / 路灯亮化工程
10. 信息化系统与集成：系统集成与弱电工程 / 软件开发与平台 / 网络通信 / 数据中心与机房 / AI与新兴技术 / 信创国产化
11. 网络安全与数据安全：网络安全防护 / 等保测评与密码 / 数据安全治理
12. 信息技术服务：运维维保服务 / IT外包与人员服务 / 数据与云服务 / 档案数字化服务
13. 后勤物业与餐饮：物业服务 / 保洁与绿化养护 / 安保服务 / 食堂餐饮与食材 / 劳务派遣 / 房屋租赁与场地
14. 医疗健康服务：体检与康复服务 / 口腔与眼科服务 / 护理陪护服务 / 医废处理与洗涤 / 公共卫生防疫
15. 安全消防与应急：消防工程与器材 / 应急与救援 / 安防监控设备
16. 专业服务：监理与招标代理 / 规划设计与造价 / 检验检测认证 / 财务审计与评估 / 法律与档案服务 / 调查普查与统计 / 科研与课题研究
17. 文化宣传与文体活动：广告印刷出版 / 媒体宣传推广 / 文化活动与演出 / 体育赛事活动 / 场馆运营与文旅
18. 农林牧渔与生态环保：农业种植与农资 / 林业与生态修复 / 畜牧渔业 / 环境治理与监测 / 环卫与垃圾处理 / 地质气象
19. 民政福利与养老：养老服务 / 福利救助 / 殡葬服务
20. 电子卖场类：电子卖场公告 / 竞价与意向公告
```

> 分类引擎为五级匹配（子类关键词 → 电子卖场规则 → 采购人行业域 → 大类语义词 → 标题形态兜底），
> 基于 18,135 条真实公告校准，覆盖率验证 100%，**无"未分类/类型待确认"**。

2. 等用户回复后，将选中结果写入**清单路径**下的 `项目类型选择.json`（仅含用户选择的大类+子类，每次研判可不同）：

```python
from scripts import excel_tool
excel_tool.save_selected_types(
    {"教学装备": [], "办公设备与家具": ["办公家具与用品"]},  # 大类: 子类列表（空=全部子类）
    path="清单路径/项目类型选择.json",  # 用步骤 1 确认的清单路径拼接
)
```
3. 向用户确认最终选中的类型范围（大类+子类），用户点头后再进入下一步。
4. 若用户说"全部"或"不限制"，则 20 个大类全部写入（子类全部），等于不做类型过滤。
5. 提示用户：选择结果已保存到 `清单路径/项目类型选择.json`，可直接用记事本增删大类与子类名称。

### 步骤 2.5：确认时间范围（初筛前必做，每次使用都要问；不强制限定）

**增量模式（--incremental）下仅当存在新增记录时询问，只问一次（R11），无新增时跳过本步。**

1. 向用户展示常用时间窗口并让其选择（**时间范围是每次初筛的可选项**，用户可随时换）：

```
请选择本次公告时间范围（回复编号，或直接说天数/不限）：
 1. 3 天内    2. 5 天内    3. 7 天内    4. 15 天内
 5. 30 天内   6. 90 天内   7. 不限（全部公告）
```

2. 将选择结果写入**清单路径**下的 `时间范围选择.json`（未配置或选"不限"时**不做时间过滤**，不强制限定）：

```python
from scripts import excel_tool
excel_tool.save_time_range(5, path="清单路径/时间范围选择.json")   # 5 天
excel_tool.save_time_range(None, path="清单路径/时间范围选择.json")  # 不限
```
3. 向用户回读确认时间范围（"本次初筛只保留 5 天内公告"或"不限时间"）。

### 步骤 3：运行流水线脚本（带三个路径参数）

在项目根目录执行（三个路径用步骤 1 确认的路径替换）：

```bash
python scripts/run_pipeline.py --list-dir "清单路径" --bid-dir "标书路径" --profile-dir "公司资料路径"
```

- **增量模式**（融合新增，默认推荐）：追加 `--incremental`，仅对新增记录初筛（已初筛自动跳过）：

```bash
python scripts/run_pipeline.py --list-dir "清单路径" --bid-dir "标书路径" --profile-dir "公司资料路径" --incremental
```

- **复评**（用户明确说"全部重新初筛""重新研判全部项目"）：追加 `--reset-status`，脚本先重置全部状态（新增待初筛/空）再按全量新增处理：

```bash
python scripts/run_pipeline.py --list-dir "清单路径" --bid-dir "标书路径" --profile-dir "公司资料路径" --reset-status
```

脚本会完成：解析 Excel（多清单合并、表头行自动探测）→ 增量过滤（仅 --incremental，按 URL 状态跳过已初筛）→ 生成企业资料摘要 → 初筛（所选时间范围内[可选项，不限也可] + 公告类型合法 + 项目类型[大类+子类]命中）→ 匹配标书（映射表→去词缀模糊→Top3 候选）→ 解析文本 → 提取评分章节 → 输出初筛结果 Excel 与待研判清单；增量模式下收尾回写状态（已初筛/已跳过，按 URL）。

- **初筛结果 Excel**：`{清单路径}/{时间}_初筛结果.xlsx`（时间格式如 `2026-08-05_1530`，各清单一个 sheet，含初筛结果/初筛原因/相关性(命中词)/来源列；增量模式下追加 初筛状态/研判状态 两列，仅含新增批次）。
- **多 Sheet 主文件特例（单文件工作流）**：清单为 `政府采购公告.xlsx`（不同地市各一个 Sheet）时，初筛结果**直接回写主文件对应 Sheet** 的「初筛结果/初筛原因/相关性(命中词)」三列（按地址链接URL 定位），**不新建** `初筛结果.xlsx` 表格；此时增量状态同样维护在主文件 Sheet 的 初筛状态/研判状态 两列。
- **中间产物**（待研判清单.json、企业资料摘要.json、标书解析文本/）写入系统临时目录 `%TEMP%/投标评估助手/`，不写入业务目录；每次运行自动覆盖。
- 若脚本报错，将中文错误信息原样反馈给用户并提供解决建议。

### 步骤 3.2：增量汇报（仅增量模式）

运行完成后按 `summary` 汇报（验收 5.2 口径）：

> "本次共读取 X 条记录：新增 N 条（已进入初筛）、跳过已初筛 M 条（其中已研判 K 条）。"

- 若存在"已初筛未研判"记录（跳过数 M 中研判状态为空的 M2 条，M2 = M − K），提示"上轮有 M2 条已初筛未完成研判，是否继续研判？"；用户说"继续研判"→ 从主文件按 URL 提取这些记录进入研判（复用待研判清单构造逻辑，**不再过初筛**）。
- 复评（--reset-status）时汇报"已重置全部状态，本次按全部新增处理"。仅"采集"或首次运行的"开始研判"按既有汇报口径。

### 步骤 3.5：企业资料占位校验（研判前必做）

- 脚本输出的 `errors` 中若含"公司介绍仍含示例内容"（仅**"某/示例"字样**触发，疑似假数据），**立即中断研判**，提示用户用记事本更新「公司资料路径/公司介绍.md」后再继续。
- **"待确认"与空值放行，不中断**：脚本仅提示缺失项，研判相关维度标注"企业资料不足，建议补充后复评"（用户有新数据时更新 md 后重跑即可）。
- 若含"企业资料未填齐（缺：…）"，向用户列出缺失项，询问是否补填；用户选择"暂不提供"时，相关维度研判标注"企业资料不足，建议补充后复评"。

### 步骤 4：读取待研判清单

读取系统临时目录 `%TEMP%/投标评估助手/待研判清单.json`，获取：

- `summary`：初筛统计（通过数量、相关性待确认数量、**类型待确认数量（type_pending_count）**、标书匹配数量、未提取到评分章节数量、已选项目类型等）。
- `company_summary`：企业资料摘要（9 节填写状态、附件业绩/人员明细——**研判唯一的企业资料入口，不直接读 md 全文**）。
- `items`：每个待研判项目（含来源文件、原始行号、项目类型、采购项目名称、预算、相关性初判与依据、`type_pending`（是否未分类待确认，默认 False）与 `type_pending_why`、是否匹配到标书、匹配方式（映射表/包含匹配/去词缀匹配）、未唯一匹配时的候选标书 Top3、标书解析文本、评分章节文本、评分章节是否缺失）。
- `unmatched_bid_files`：文件名不含项目名称、未匹配到任何项目的标书。
- `bid_candidates`：未匹配标书的自动预识别结果（候选项目短语 + 关联到的清单项目，建议 8），随未匹配清单一起在对话中呈现。
- `bid_mapping`：本次生效的手动映射（`标书路径/标书映射.json`，建议 10）。
- `screening_excel`：初筛结果 Excel 的完整路径。

初筛汇报格式（markdown 表格，**第一列为项目类型**）：

```
| 项目类型 | 公告时间 | 公告类型 | 采购项目名称 | 相关性(命中词) | 初筛结果 | 初筛原因 | 来源 |
```

- 初筛汇总表可直接调用脚本函数生成（`excel_tool.build_screening_md_table(带初筛列的DataFrame)`），
  也可依据 `items`/初筛结果 Excel 内容自行整理；**"相关性(命中词)"列必须始终展示，不可省略**。
- 相关性为**软标记**：不参与过滤。"待确认"的项目全部保留，在对话中一次性列出向用户确认（"以下 N 个项目相关性存疑，是否保留研判？"），用户确认后参与研判，不得静默剔除。
- **项目类型无未分类兜底**：v3.2 五级匹配（子类关键词 → 电子卖场规则 → 采购人行业域 → 大类语义词 → 标题形态兜底）保证每个项目都有明确大类归属（18,135 条真实公告验证 100% 命中），无 `type_pending`/待人工确认；未命中已选类型范围的项目直接标记"项目类型不符"过滤。

### 步骤 5：处理未匹配项

- 读取 `%TEMP%/投标评估助手/待研判清单.json` 的 `unmatched_bid_files`（未匹配标书）与 `bid_candidates`（自动预识别结果）：
  - 脚本已对每个未匹配标书解析**标题区前 500 字**，提取含"项目/采购/招标"特征的**候选项目短语**，并尝试与清单项目关联（"匹配到清单项目"）；
  - 在对话中**完整展示**：标书文件名 + 候选短语 + 匹配到的清单项目（无候选时明确提示"无候选"），请用户点选确认或直接指定。
- 用户确认后，**立即调用 `add_bid_mapping()` 将映射写入标书路径下的 `标书映射.json`**（随标书走；下次运行直接复用，不再询问）：

```python
import os
from scripts import bid_mapping
bid_mapping.add_bid_mapping(
    "202609.doc",
    "衢州市公安局衢江分局杜泽空调采购项目",
    path=os.path.join(标书路径, "标书映射.json"),  # 用步骤 1 确认的标书路径拼接
)
```

- 写入后提示用户：映射已保存到 `标书路径/标书映射.json`（`{"标书文件名": "采购项目名称"}`，可用记事本编辑），下次运行流水线自动复用、不再列入未匹配。
- 用户暂不指定的文件**单独列出，不阻塞其他项目**的研判。

### 步骤 6：确认企业资料（研判前必做）

**初筛通过后、正式研判前，必须先确认企业资料已就绪。这是研判准确性的前提。**

1. 读取 `%TEMP%/投标评估助手/企业资料摘要.json`（或调用 `company_profile.build_summary(md_path=None, attachments_dir=公司资料路径)`），检查以下 **9 项企业资料**是否已填写（冒号后有实际内容，而非空白）：

```
 1. 公司基本信息（名称、注册资本、成立年限、所在地）
 2. 公司业务（主营业务、服务领域）
 3. 资质证书（ISO认证、行业许可、专项资质）
 4. 历史业绩（近三年同类项目、金额、客户）
 5. 主销产品与品牌
 6. 厂家合作关系（授权经销商、授权书）
 7. 人员团队（总人数、持证人员、项目经理）
 8. 财务能力（保证金上限、可接受账期、流动资金）
 9. 其他说明（本地服务能力、售后团队）
```

2. 将已填写和未填写的项**摘要展示给用户**（核对表）：

```
## 企业资料核对表（9 项）
- 已提供：公司业务、资质证书、历史业绩、厂家合作关系
- 还需补充：人员团队、财务能力、公司基本信息
- 附件：业绩表.xlsx（10 条，总金额 1500 万）｜ 人员社保表.xlsx（未放置）
请提供以上缺失信息，或直接告诉我，我来帮您填入 公司资料路径/公司介绍.md。
```

3. 用户在对话中口述补充时，**按 9 项逐项引导提问**（一次问 1-2 项），不要一次性让用户倒豆子：先问公司基本信息，再逐项问业务/资质/业绩/产品/厂家/人员/财务/其他。AI 将内容整理后写入「公司资料路径/公司介绍.md」对应小节，写入后**回读校验**：逐节确认关键字段非空（如人员节的公司总人数/持证人员/项目经理数），漏填的项在对话中追问一次。
4. 全部关键项确认后，向用户展示最终企业资料摘要，用户点头后再进入研判。
5. 若用户表示"暂时不提供某些项"，标注为"待确认"，研判时该维度给出"企业资料不足，建议补充后复评"的提示，但不阻塞已足够研判的项目。
6. **附件约定**（可选但强烈建议，用于评分客观项逐条核对）：
   - 「公司资料路径/业绩表.xlsx」、「公司资料路径/人员社保表.xlsx」：**表头按用户实际提供的格式，脚本原样保留全部列与数据**（不做列名归整、不删减字段）；字段含义按实际表头理解，研判时逐条引用；
   - 业绩表可调用 `excel_tool.classify_project(项目名, 采购人)` 自动标注「项目大类/项目子类/命中关键词」列（与初筛同一套 20 大类 × 52 子类体系），研判比对同类业绩时直接按子类列筛选；
   - 附件缺失时，业绩/人员核对需用户在对话中提供或确认。

### 步骤 7：逐项进行 AI 研判

对每个**已匹配到标书**的项目，按「八、AI 研判框架」研判（**资格条件核对 → 客观分扣分核算 → 提示**），**先给结论，再给理由与提示**。

- 每次只载入**当前项目的标书文本 + 评分章节文本 + `%TEMP%/投标评估助手/企业资料摘要.json`**，研判完立即输出，避免一次塞入全部导致上下文过长。
- **输出顺序固定**：① 结论（适合/不适合 + 确定扣分合计）→ ② 理由与依据（资格条件核对表 + 评分客观分扣分核算表）→ ③ 提示（需核实项潜在扣分、准备事项）。
- 评分核对**强制执行、不得跳过**：
  - `scoring_section` 非空时，逐条识别其中**客观分项**（企业资质、业绩、人员、认证证书、发票、中标通知书等），对照企业资料摘要逐条核算扣分；
  - `scoring_section` 为空（`scoring_missing=True`）时，**必须从当前项目标书全文兜底查找评分细则**再核对，不得以"未提取到"为由跳过；
  - **「评分条款要求」必须逐字引用标书原文条款，不得概括或截断**（含注、证明材料要求、分值等完整内容）；多项条款混在同一段落时逐条切分，不得把多条评分项合并显示。
- 扣分核算判定为三态：**满足（扣 0）/ 不满足（扣该项全部分值）/ 需核实材料（暂不计入确定扣分，在提示中说明潜在扣分）**。

### 步骤 8：整理研判报告（md 写入标书路径）

- **研判结论不再回写 Excel。** 全部项目研判完毕后，AI 将**全部研判结论整理为一个 markdown 报告**写入**标书路径**：`{标书路径}/{时间}_研判报告.md`（时间格式如 `2026-08-05_1530`）。
- **报告不包含初筛汇总**，只含逐项目研判；每个项目结构见「九、输出格式」：**结论 → 理由与依据（资格条件核对表 + 客观分扣分核算表）→ 提示**。
- 报告使用 UTF-8 编码保存（`open(path, "w", encoding="utf-8")`）。
- 逐项目研判部分按「九、输出格式」逐条输出，未匹配到标书的项目不写入研判报告（在对话中已单独列出）。
- 写入完成后告知用户报告文件位置，并提示可分享该 md 供多人协作查看。

### 步骤 8.5：研判完成回写状态（增量机制收尾，方案 4.3）

- 全部项目研判完毕后，按**已研判 URL 列表**批量回写「研判状态=已研判」（一次性回写，避免多次打开大文件；按 URL 定位行，禁止按行号）：

```python
from scripts import status_tool
status_tool.write_state(清单路径, [{"url": "https://zfcg.czt.zj.gov.cn/site/detail?articleId=xxx", "研判状态": "已研判"}, ...])
```

- 状态载体：主文件（含"初筛状态"列）自动回写其 研判状态 列；用户自备清单回写 清单路径/增量状态.json（不改写用户清单文件）。
- 回写后提示用户：下次"开始研判"将自动跳过这些已研判记录（可在主文件 Excel 中查看 研判状态 列）。

### 步骤 8.6：复评（用户明确指令触发，默认跳过）

- 仅当用户明确说「全部重新初筛」「重新研判全部项目」时执行（边界 6 口径：可复评但默认跳过）；任何"开始研判/采集并初筛"均不重置状态。
- 执行方式：`status_tool.reset_status(清单路径)` 将全部记录重置为「初筛状态=新增待初筛、研判状态=空」，再运行 `run_pipeline --reset-status`（脚本内置先重置再按全量新增处理），下一轮全量初筛。

## 四、初筛规则（由脚本执行）

时间范围为**可选项，不强制限定**（未配置或选"不限"时不做时间过滤）；以下规则在配置了时间范围时生效：

1. **时间条件**：公告挂出时间在所选天数以内（如 5 天）；未配置时间范围时跳过该条件。
2. **类型条件**：公告类型为「采购项目公告」或「电子卖场公告」；其中**电子卖场公告（竞价/询价/网上超市）平台无招标文件、无法投标，初筛阶段直接剔除**（初筛结果=「电子卖场」，原因=「电子卖场公告（平台无招标文件，无法投标）」）。
3. **项目类型条件**：项目名称命中 `清单路径/项目类型选择.json` 中用户本次确认启用的类型关键词（如教学装备、办公设备与家具、信息化系统与集成）；v3.2 五级匹配保证每个项目都有明确类型归属，未命中已选类型范围 → 过滤并在「初筛原因」列标记"不在已选项目类型范围"；**无"未分类/类型待确认"**（无兜底）。
4. **相关性条件（软标记，不参与过滤）**：脚本结合公司介绍与「采购项目名称」做关键词初判，输出「相关 / 待确认」及命中词依据（"相关性(命中词)"列）。"待确认"项目**保留在候选清单**，由 AI 在对话中一次性列出向用户确认是否保留研判；**不得静默剔除**（关键词覆盖不全导致的误杀由人工确认兜底）。

**项目类型分类规则补充（脚本内置，无需用户操作）**：
- **五级匹配（v3.2，无兜底）**：子类关键词 → 电子卖场公告规则 → 采购人行业域（学校/教育局→教学装备、医院/疾控→医疗设备与医用耗材、公安/法院→安全消防与应急等）→ 大类语义词 → 标题形态兜底（含"工程/施工"→建筑工程、含"服务/咨询"→专业服务、含"项目"→专业服务）；18,135 条真实公告验证 100% 命中；
- **食堂劳务外包互斥**：命中"食堂餐饮与食材"子类的项目，不再重复标注"劳务派遣"子类（同一项目避免重复打标）；
- **电子卖场公告**：公告类型为"电子卖场公告"即归入"电子卖场类"（不受项目名限制）；分类后仍在初筛阶段整体剔除（见上文类型条件）。

## 五、文件匹配规则

- 匹配顺序：**手动映射表优先**（`标书路径/标书映射.json`，AI 在对话中经用户确认后自动写入）→ 自动模糊匹配。
- 自动模糊匹配：Word 文件名（去扩展名）包含「采购项目名称」，或互为包含；不中时**去掉词缀**（招标文件/竞争性磋商文件/磋商文件/定稿/正式稿/最终版/电子标/招标公告/采购文件等）后再包含匹配。
- 仍无法唯一匹配（含多个文件同时命中）时，返回**候选匹配 Top3**（公共前缀/公共子串相似度）在对话中请用户确认，**不自动猜测**。
- 未匹配标书由脚本自动预识别标题区（前 500 字）候选项目短语（建议 8），随未匹配清单一起呈现，用户只需点选确认。

## 六、企业资料与项目类型配置

- 三个路径（清单路径/标书路径/公司资料路径）由**用户每次研判时提供**（步骤 1），不落盘记忆，产物就近放置。
- 研判背景依据读取「公司资料路径/公司介绍.md」（支持 `.md`/`.txt`，AI 代写也写入该路径），脚本汇总生成企业资料摘要（`%TEMP%/投标评估助手/企业资料摘要.json`，9 节填写状态 + 附件明细），**AI 研判只引用摘要，不直接读 md 全文**。
- 文档含 9 个小节：公司基本信息、公司业务、资质证书、历史业绩、主销产品与品牌、厂家合作关系、人员团队、财务能力、其他说明。默认全部空白，由用户在初筛后、研判前在对话中口述补充，AI 代为写入并回读校验。
- 可选附件（用于评分客观项逐条核对）：「公司资料路径/业绩表.xlsx」、「公司资料路径/人员社保表.xlsx」（每行一条业绩/一名员工）。**公司介绍含"某/示例"字样时，脚本会中断提示，防止基于示例数据研判；"待确认"与空值放行，仅提示缺失项。**
- **项目类型 20 大类 + 52 细分子类模板固定在 Skill 内部**（步骤 2 列表 + `scripts/excel_tool.py` 内置常量，不随业务路径；无"其他"兜底类，五级匹配 100% 命中）；每次研判由 AI 先展示全部 20 大类让用户选择，再对每个大类选择子类（默认全部），选择结果写入「清单路径/项目类型选择.json」（大类→子类结构），每次研判可不同，无需改任何代码。

## 七、异常与容错

- 所有异常（缺文件、缺列、格式错、匹配不到）必须返回**中文可读信息**并给出解决建议。
- 检测到 `.xls` 或 `.csv` 时，提示用户"请另存为 .xlsx 格式后重试"。
- 检测到 `.doc` 老格式标书时，优先用 Word 自动转换为 .docx 后解析；本机无 Word 或未装 pywin32 时，提示"该文件为 .doc 老格式，请用 Word 另存为 .docx 后重试"。
- 误放 `.pdf` 时尽力解析，不阻塞流程。
- 单个项目失败不影响其他项目研判。
- 未提取到评分章节时，AI 从标书全文自行查找评分表，不得跳过客观项核对。
- 初筛结果 Excel 被 Excel 占用时，提示用户关闭后重试，不静默失败。
- 中间产物一律写入系统临时目录 `%TEMP%/投标评估助手/`，业务目录（三个路径）不残留。
- 采集/状态相关坑位（融合新增，沿用采集器既有 + status_tool）：
  - 所有 openpyxl 加载一律不带 `read_only=True`（Python 3.13 崩溃坑位，禁止回归）；
  - 主文件/批次写入前检查锁文件 `~${文件名}`，被 Excel 打开时提示"错误：主文件被 Excel 打开，请先关闭 Excel 再运行"，不影响已完成的初筛/研判产物；
  - 状态 JSON（增量状态.json）损坏时提示"增量状态文件无法读取，本次按全部新增处理"，不崩溃；
  - 清单缺少"地址链接URL"列时降级全量处理，提示"清单缺少地址链接URL列，无法增量，本次全量处理"；
  - 单次采集 >200 条 / 补采 >2 天时拆批执行；API 失败日期 AI 重试一次，仍失败给中文提示；当日 18:00 前提示"下午公告可能未发布完，建议 18:00 后补采"；
  - 个体交叉引用 >50 条自动跳过 Layer 1–3（仅用主文件继承），打印说明不报错。

## 八、AI 研判框架（资格条件核对 + 客观分扣分核算 + 提示）

判定该项目 **是否适合本公司参标** 时，按以下三步执行：

**第一步（强制）：资格条件（硬门槛）核对。** 从标书「资格要求/特定资格要求/资格证明文件」部分提取全部资格条件（如特定资质、中小企业要求、联合体限制、业绩门槛等），对照企业资料逐条三态判定：**任一资格条件不满足 → 直接判"不适合"（不具备投标资格），不再继续核算评分**。

**第二步（强制）：评分客观分扣分核算。** 从评分章节/标书全文识别全部**客观分项**（企业资质、业绩、人员、认证证书、发票、中标通知书等，评分表中标注"客观分"或可客观核对的分项），对照企业资料逐条判定并**核算扣分**：

| 客观分项 | 分值 | 评分条款要求（原文） | 公司现状 | 判定 | 扣分 |
|---------|-----|--------------------|---------|------|------|
| 资质证书（如 ISO20000/27001） | X 分 | 持证得 X 分 | 企业资料摘要：… | 满足/不满足/需核实材料 | 0 / X / 待定 |

- **满足**：扣 0 分；**不满足**：扣该项全部分值；**需核实材料**：暂按待定处理，在"提示"中说明潜在扣分（若该材料缺失则扣 X 分），不直接计入确定扣分。
- **「评分条款要求」必须逐字引用标书原文条款**（含注、证明材料要求、分值等完整内容），不得概括或截断；多项条款混在同一段落时逐条切分。
- 主观分项（实施方案、演示、样品等）不参与扣分核算，在"提示"中单独说明准备要求。
- **扣分判定线**：客观分确定扣分合计 **>2 分 → 判"不适合"（不建议参加，理由中列出各扣分来源）**；**≤2 分 → 判"适合"**（提示中列出需核实项及其潜在扣分、可补强项）。
- 行业维度参考（用于识别客观分项归属，不单独否决）：**资质匹配**（标书要求的资质证书公司是否持有）、**业绩要求**（同类业绩数量/金额/年限是否达标）、**人员团队配置（含社保核实）**（持证人员/团队人数；标书要求人员资质附带社保条件时必须核实社保缴纳情况）。

**第三步（提示维度，仅标注不作判定）：** 产品厂家支持（是否授权经销商、能否取得厂家授权书）、付款方式（付款比例、账期、质保金比例）、保证金（金额占比、是否可承受）、技术参数要求（软硬件规格、服务能力）、投标截止时间（剩余时间是否来得及准备并提交标书）——仅在标书存在明显异常（如截止时间已过、保证金异常高）时输出一句提示，不参与结论。

**判定原则**：资格条件任一不满足 → "不适合"（无投标资格）；资格条件通过后按客观分扣分核算：确定扣分合计 >2 分 → "不适合"（不参加），≤2 分 → "适合"（提示需核实项及潜在扣分）。理由必须**引用具体依据**（如"资质不符""业绩不达标""人员/社保不满足""客观分扣 X 分"）。

## 九、输出格式

### 研判报告（md 文件，写入标书路径 `{时间}_研判报告.md`）

报告结构：**只含逐项目研判，不含初筛汇总**。每个项目按"结论 → 理由与依据 → 提示"输出。

### 逐项目研判（markdown，逐项目输出）

```
## 【采购项目名称】
- 项目类型：教学装备（教育信息化设备）
- 预算：… 元；采购人：…；公告：…
- 来源：清单文件名 / Excel 行号 / 标书文件名

### 一、结论                      ← 先给结论
**适合（可参加）** / **不适合（不建议参加）**
确定扣分合计：X 分

### 二、理由与依据                ← 再给依据
**（一）资格条件核对（硬门槛）**
| 资格条件 | 招标文件要求（原文） | 公司现状 | 判定 |
|---------|--------------------|---------|------|
| … | … | … | 满足 / 不满足 / 需核实材料 |

**（二）评分客观分扣分核算**      ← 条款要求必须逐字引用原文
| 客观分项 | 分值 | 评分条款要求（原文） | 公司现状 | 判定 | 扣分 |
|---------|-----|--------------------|---------|------|------|
| … | … | …（完整原文，含注/证明材料要求/分值） | … | 满足/不满足/需核实材料 | 0 / X / 待定 |

（结论依据：资格条件情况 + 各扣分来源说明）

### 三、提示                      ← 最后提示
需核实项潜在扣分 / 可补强项 / 保证金、付款、截止时间等明显异常
```

- 结论必须简洁明确；评分客观分核算**不得遗漏、不得跳过**（`scoring_section` 为空时从全文兜底查找）。
- **评分条款要求必须逐字引用标书原文**，不得概括、不得截断、不得把多条评分项混在一起。
- 确定扣分合计 >2 分 → "不适合"并列出各扣分来源；≤2 分 → "适合"并提示需核实项。
- 初筛结果 Excel（`清单路径/{时间}_初筛结果.xlsx`）与研判报告 md（`标书路径/{时间}_研判报告.md`）时间戳格式一致（`2026-08-05_1530`），便于对应查看。

## 十、示例（few-shot）

### 示例 1：适合（资格条件通过，客观分确定扣分 ≤2 分）

> 企业资料：主营办公设备销售与售后，持有 ISO9001/ISO14001，近三年中标 8 个政府办公设备采购项目（累计金额 1200 万），公司 35 人含 3 名项目经理（均连续缴纳社保满 24 个月、社保缴纳单位为本公司），是 A 牌、B 牌厂商授权经销商，可承受保证金 50 万、账期 60 天。

- 项目：某市财政局 2026 年度办公设备采购项目（项目类型：办公设备与家具；来源：杭州市清单.xlsx 行 12）
- 标书要点：特定资格要求无；评分细则：价格 40 分；企业资质 15 分（ISO9001 认证得 15 分，无则 0 分）；业绩 25 分（近三年同类业绩≥3 个得满分，每少 1 个扣 5 分）；人员 20 分（驻场项目经理 1 名 + 近 6 个月社保证明得满分，否则 0 分）；实施方案等主观分 0 分（不计入客观扣分）。
- 结论：**适合（可参加）**，确定扣分合计 0 分
- 理由与依据：
  - 资格条件核对：特定资格要求无 → 满足
  - 评分客观分扣分核算（条款原文逐字引用）：企业资质「持有 ISO9001 认证，得 15 分」→ 满足（扣 0）；业绩「近三年同类业绩≥3 个得满分，每少 1 个扣 5 分」→ 满足（8 个同类项目，扣 0）；人员「驻场项目经理 1 名并提供近 6 个月社保缴纳证明」→ 满足（3 名项目经理均连续参保、单位为本公司，扣 0）
- 提示：需核实项无；付款、保证金、截止时间未见明显异常。

### 示例 2：不适合（客观分确定扣分 >2 分）

> 企业资料：主营办公设备销售与售后，持有 ISO9001，近三年 5 个同类业绩，公司 12 人无持证人员、未全员缴纳社保，是 A 牌授权经销商。

- 项目：某区教育系统智慧校园设备采购及安装项目（项目类型：教学装备；来源：温岭清单.xlsx 行 8）
- 标书要点：特定资格要求无（专门面向中小企业，公司符合）；评分细则：价格 40 分；企业资质 10 分（ISO9001 认证得 10 分）；业绩 20 分（近三年同类业绩≥3 个得满分，每少 1 个扣 5 分）；人员 30 分（项目经理持一级建造师证 + 近 6 个月社保缴纳证明，社保缴纳单位须为投标人）。
- 结论：**不适合（不建议参加）**，确定扣分合计 30 分
- 理由与依据：
  - 资格条件核对：面向中小企业符合 → 满足
  - 评分客观分扣分核算（条款原文逐字引用）：企业资质「持有 ISO9001 认证，得 10 分」→ 满足（扣 0）；业绩「近三年同类业绩≥3 个得满分，每少 1 个扣 5 分」→ 满足（5 个同类项目，扣 0）；人员「项目经理持一级建造师证并提供近 6 个月社保缴纳证明（社保缴纳单位须为投标人），得满分 30 分」→ 不满足（公司无一级建造师持证人员，扣 30 分）
- 扣分来源：人员项扣 30 分（>2 分判定线）→ 不建议参加
- 提示：若公司计划考取一级建造师并规范参保后再评估此类项目。

## 十一、约束边界（不越界）

- **自动下载标书为可选能力（需求变更 2026-08-15）**：仅在用户明确要求时执行；登录由用户本人在浏览器窗口完成，**脚本不接触、不落盘账号密码**；下载范围限于待研判清单内项目；电子卖场公告等无「获取采购文件」入口的项目自动跳过。
- **不输出长篇幅论述**：报告只输出结论、资格条件与扣分核算表、提示；评分条款原文完整引用属必要内容。
- **不做打分排名**：只核算客观分扣分（确定扣分合计 >2 分不参加），不给出分数或与其他投标人比较排名。
- **不修改需求与方案**：不擅自改变已确认的需求和架构方案。
- **不阻塞**：单个项目失败或无法匹配时，单独提示，不中断其他项目。
- **不擅自决策**：对需求或架构有疑问时，交回项目经理处理，不自行更改
