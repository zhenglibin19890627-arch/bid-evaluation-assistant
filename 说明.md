# 投标机会评估助手 · 使用说明

> 面向零代码用户。你不需要写任何代码，只需按下面的步骤操作即可。

---

## ⚡ 30 秒快速上手

1. **安装依赖**：双击 `安装依赖.bat`，等待完成。
2. **准备文件**：把投标机会清单 Excel、下载的标书、公司资料放在你喜欢的文件夹里（任意位置都行）。还没有清单？直接说"采集"，技能会从浙江政府采购网自动生成清单（主文件）。
3. **开始研判**：在 Qoder 对话中说「开始研判」，然后**告诉 AI 三个文件夹的路径**（清单路径、标书路径、公司资料路径）。

完成！初筛结果会输出到清单路径下的 Excel，研判报告会输出到标书路径下的 Markdown 文件。

---

## 📌 这是什么

这个工具帮你从政府采购云平台的海量投标机会里，**自动筛选出值得关注的采购项目**，并结合自己公司的实力，对下载下来的**招标文件（标书）**进行 AI 研判，告诉你每个项目**"适不适合参标、为什么"**，最后生成初筛结果 Excel 和研判报告，方便你和同事一起查看。可选的"采集"功能会按地市自动抓取政府采购公告生成清单，省去手动导出。

### 核心流程

```
（可选）说"采集"自动生成清单 → 告诉 AI 文件路径 → 确认项目类型 → 说"开始研判" → 查看初筛结果 / 研判报告
```

**每天只需说"开始研判"，已研判的项目不会再出现**（增量机制，见下文）。

---

## 一、三个路径是什么（文件放哪里都行）

本项目**不限制文件放在哪个文件夹**，每次研判时你把下面三个文件夹的路径告诉 AI 即可：

| 路径 | 放什么进去 | 说明 |
|------|-----------|------|
| **清单路径** | 投标机会清单 Excel（.xlsx） | 从政府采购云平台导出的清单，**支持放多个地市的清单一起处理**（表头在第几行都能自动识别）；也可以是本技能"采集"自动生成的主文件（`{地市}政府采购公告.xlsx`） |
| **标书路径** | 下载的招标文件（.docx/.doc/.pdf） | 一个项目对应一个文件；可以和你放清单的文件夹是同一个 |
| **公司资料路径** | 公司介绍.md、业绩表.xlsx、人员社保表.xlsx | 公司信息（可让 AI 代写）；业绩/人员明细表可选，用于评分逐条核对 |

> 三个路径可以相同（所有文件放一个文件夹）也可以不同，完全由你决定，不落盘记忆、每次重新告诉 AI。

### 产物自动放到哪里

| 产物 | 位置 |
|------|------|
| 采集主文件（`{地市}政府采购公告.xlsx`） | **清单路径**下（含 初筛状态/研判状态 两列，打开 Excel 即可查看每条记录的处理进度） |
| 初筛结果 Excel（`2026-08-05_1530_初筛结果.xlsx`） | **清单路径**下（多个清单各一个工作表） |
| 研判报告（`2026-08-05_1530_研判报告.md`） | **标书路径**下 |
| 项目类型选择（`项目类型选择.json`） | **清单路径**下 |
| 标书映射（`标书映射.json`） | **标书路径**下（随标书走） |
| 中间文件（待研判清单、企业资料摘要、标书解析文本） | 系统临时目录 `%TEMP%/投标评估助手/`（不用管，每次自动覆盖） |

> **多 Sheet 主文件（单文件工作流）特例**：若你的清单是 `政府采购公告.xlsx`（不同地市各占一个 Sheet），初筛结果会**直接回写主文件对应 Sheet** 的「初筛结果/初筛原因/相关性(命中词)」三列，**不会**另建 `初筛结果.xlsx`；打开主文件就能看到每条记录的初筛结论。

#### 主文件状态列含义

| 列 | 取值 | 含义 |
|----|------|------|
| 初筛状态 | 新增待初筛 / 已初筛 | 新采集并入的记录自动标"新增待初筛"；处理过初筛后标"已初筛" |
| 研判状态 | （空）/ 已研判 / 已跳过 | 空=初筛通过待研判；已研判=研判完成；已跳过=初筛未通过无需研判 |

#### 增量机制（重点）

- 以**公告链接（地址链接URL）**为唯一键：**初筛状态≠已初筛 的记录 = 新增**，进入本次初筛；**已初筛自动跳过初筛，已研判自动跳过研判**；
- **每天直接说"开始研判"即可**，已研判的不会再出现，AI 会在对话中说明本次"新增几条、跳过几条"；
- 用你自己导出的清单也一样：状态记录在 清单路径/增量状态.json，**不会改动你的清单文件本身**；
- 想从头全部重来一遍？说**"全部重新初筛"**或**"重新研判全部项目"**即可（默认不会自动重置）。

---

## 二、第一步（可选）：采集公告（自动生成清单）

> 没有现成清单时，让技能从**浙江政府采购网**按地市自动采集公告并生成主文件清单；已有清单/主文件的用户直接跳过本节，去"第四步：开始研判"。

1. 在对话中说：**"采集"**（或"采集公告""采集并初筛"）。
2. AI 会列出**浙江 11 地市**（杭州/宁波/温州/嘉兴/湖州/绍兴/金华/衢州/舟山/台州/丽水）让你选择，回复地市名称即可（如"丽水市"；每次采集确认一次，不落盘）。
3. 告诉 AI **清单路径**（采集的主文件就放在这里；也可以和标书、公司资料共用一个文件夹）。
4. AI 自动执行三步，你只需等待：
   - **分析缺失日期**：扫描主文件近 10 天缺哪些日期的公告（首次使用主文件不存在时，自动采集最近 3 天）；
   - **自动采集**：抓取该地市 9 类采购公告（超过 2 天自动分批进行，耗时约几分钟，请保持网络畅通）；
   - **合并去重**：按公告链接去重、按时间倒序合并进主文件 `{清单路径}/{地市}政府采购公告.xlsx`。
5. 完成后 AI 汇报"本次新增 N 条公告（跳过重复 M 条）"。
6. 说**"采集并初筛"**可采集后直接进入研判；仅说"采集"则之后说**"开始研判"**继续。

> 小提示：当日 18:00 前采集今天的数据，下午的公告可能还没发布完，建议 18:00 后补采。

---

## 二·五 自动下载招标文件（可选）

> 不想在政采云上一个个手动下载标书？在对话中说**"下载标书"**，AI 会从平台自动取回待研判项目的招标文件，放入标书路径，并自动记住"文件↔项目"对应关系。

**只需两步准备（都只做一次）：**

1. 安装浏览器自动化依赖（一条命令）：
   ```bash
   pip install -r requirements_download.txt
   ```
2. 登录一次政采云：在对话中说"登录政采云"，AI 会弹出一个浏览器窗口，**你在窗口里自己输入账号密码登录**（密码只有你知道，脚本不接触）；登录态加密保存在本机，以后不用重复登录。

**之后每次下载**：直接说"下载标书"即可。AI 会：
- 逐条处理待研判项目：已获取的直接下载；未获取的自动填写申请表单（联系人信息存本机 `申请信息.json`）并提交获取申请后下载；
- 下载的招标文件自动放入**标书路径**，并写入 `标书映射.json`（文件名→项目名），后续"开始研判"自动对应，无需手动匹配；
- 电子卖场竞价/询价类公告没有招标文件，自动跳过。

> ⚠️ 下载期间会短暂弹出浏览器窗口，请不要手动操作它；批量下载约每项 20 秒，请耐心等待 AI 汇报。
> ⚠️ 登录过期（平台要求重新登录）时，AI 会提示你再运行一次登录。

---

## 三、第二步：安装依赖（只需一次）

> 这一步只需要在第一次使用时做一次。如果已经装过，可以跳过。

1. 打开电脑上的"命令提示符"或"PowerShell"（按 `Win` 键，输入 `cmd` 回车）。
2. 输入下面命令并回车，进入项目文件夹：

```bash
cd <本项目所在文件夹>
```

3. 输入下面命令并回车，等待安装完成（出现"Successfully installed"表示成功）：

```bash
pip install -r requirements.txt
```

4. 【可选】如果常收到 `.doc` 老格式标书（且电脑已装 Microsoft Word），再执行一行开启自动转换：

```bash
pip install pywin32
```

> 安装完成后，以后使用就不需要再装依赖了。

---

## 四、第三步：准备文件

### 1. 准备投标机会清单
- 从政府采购云平台导出机会清单，保存为 **Excel 工作簿（.xlsx）** 格式；**或用"采集"自动生成主文件清单（见"第一步（可选）：采集公告"）**。
- 放到你选定的**清单路径**文件夹里。
- 清单至少需要包含这几列：**公告时间、公告类型、采购项目名称**（通常自动就有）。

> ⚠️ 如果导出的是 `.xls` 或 `.csv`，请用 Excel 打开后「文件 → 另存为 → Excel 工作簿(.xlsx)」，再放进去。
> 采集生成的主文件含 初筛状态/研判状态 两列，直接使用即可（状态由技能自动维护，不用管）。

### 2. 准备招标文件（标书）
- 从平台上把你想研判的招标文件下载下来（.docx 格式）。
- 放到你选定的**标书路径**文件夹里。
- **文件名最好包含采购项目名称**，这样工具能自动对应到 Excel 里的那一行。
- 一个项目对应一个文件。

### 3. 准备公司介绍（研判前必填）
- **公司资料路径**下放一个 `公司介绍.md`（可让 AI 代写：对话里口述信息，AI 帮你写入该文件夹）。
- ⚠️ 文件里若有"某""示例"等字样，工具会提示你先更新，防止基于示例数据研判；"待确认"或留空的项**不会中断**，只会在对话中提示缺失，研判时标注"企业资料不足，建议补充后复评"。
- **建议（可选）**：把业绩明细放进 `业绩表.xlsx`、人员社保明细放进 `人员社保表.xlsx`（**表头按你们实际表格的格式即可，所有列都会被完整保留**），AI 会逐条核对评分要求，减少漏项。

---

## 五、第四步：开始研判

1. 打开 Qoder，进入本 Skill 的对话。
2. 直接说：**"开始研判"**（或"开始评估""帮我看看这些标书适不适合"）。**每天直接说"开始研判"即可，已研判/已初筛的项目会自动跳过**，只处理新增记录，AI 会告诉你本次新增几条、跳过几条。
3. AI 会先问你要**三个文件夹的路径**（清单/标书/公司资料），并回读确认路径下有哪些文件。
4. AI 让你选择本次要筛选的**项目类型大类**（20 种，多选），再对每个大类选择**细分子类**（52 个，多选，不选=全部），选择结果会保存到清单路径的 `项目类型选择.json`。
5. AI 再让你选择**公告时间范围**（3/5/7/15/30/90 天或不限），保存到 `时间范围选择.json`；**不限制也完全可以**（不限时不做时间过滤）。
5. AI 自动执行：
   - 初筛：按所选时间范围（不限也可）、类型合法、与公司相关的项目，输出 **初筛结果 Excel**（清单路径下）；
   - 匹配：把标书文件对应到清单里的项目；
   - 确认企业资料：问你要公司资质、业绩、团队等信息；
   - 研判：先核对**资格条件**（硬门槛），再逐条核算**评分客观分扣分**（确定扣分合计 >2 分不建议参加），给出"适合 / 不适合 + 理由"；
   - 整理：把全部研判结论整理为 **研判报告 md**（标书路径下，不含初筛汇总）。
6. 完成后，AI 会告诉你报告位置：初筛结果在清单路径的 Excel，研判报告在标书路径的 md 文件，可以直接分享给同事。

---

## 六、研判看什么（资格条件 + 客观分扣分 + 提示）

AI 分两步判断一个项目适不适合你公司：

**第一步：资格条件（硬门槛）**。标书要求的企业资质、业绩门槛、中小企业等资格条件，**任一不满足就没有投标资格，直接判"不适合"**。

**第二步：评分客观分扣分核算**。把标书评分细则里的客观分项（企业资质、业绩、人员、认证证书等）逐条对照公司情况核算：
- 满足 → 不扣分；不满足 → 扣掉该项分值；需核实材料 → 暂不计入确定扣分（提示中说明潜在扣分）
- **确定扣分合计 >2 分 → 不建议参加（判"不适合"）；≤2 分 → 可参加（判"适合"）**

**资格与业绩、人员等维度参考**：标书要求的资质证书公司是否持有、同类业绩（数量/金额/年限）是否达标、持证人员与团队人数（**标书要求人员资质附带社保时必须核实社保缴纳情况**）。

另外，**产品厂家支持、付款方式、保证金、技术参数、投标截止时间** 仅作提示、不参与"适合/不适合"结论——仅当标书存在明显异常（如截止时间已过、保证金异常高）时，AI 会额外提醒一句。

AI 会**专项核对标书评分细则**中的客观分项（企业资质、业绩、人员），逐条引用原文条款确认本公司是否满足得分要求，确保不遗漏标书中的得分项。

---

## 七、常见问题（FAQ）

### Q1：提示"xlsx 格式不对 / 请另存为 .xlsx"
- 说明你放的是 `.xls` 或 `.csv`。用 Excel 打开，点「文件 → 另存为 → Excel 工作簿(.xlsx)」，再放回清单路径。

### Q2：提示"缺少列：公告时间、公告类型、采购项目名称"
- 说明清单里没有这些列。请确认是从平台正常导出的清单，或手动补上这些列。

### Q3：提示"标书文件和项目对不上 / 无法匹配"
- 工具会先按**映射表**（`标书路径/标书映射.json`）匹配，再做文件名模糊匹配（自动去掉"招标文件/定稿/正式稿/最终版"等词缀）。
- 仍对不上时，会列出**候选 Top3** 和未匹配标书的**标题区预识别候选**，告诉 AI 这个文件对应哪个项目，AI 会帮你确认并**自动记住**（写入 `标书路径/标书映射.json`），下次直接复用。

### Q4：某个标书提示"格式不支持"
- 本工具支持 `.docx`、`.doc`（老格式，装好 Word 后自动转换）、`.pdf`（尽量读取）。其他格式请用 Word 另存为 `.docx`。
- 若提示"请用 Word 另存为 .docx 后重试"：说明本机没有 Word 或未装 pywin32（见安装步骤里的可选说明），用 Word 打开文件后「另存为 → Word 文档(.docx)」即可。

### Q5：清单路径里没有初筛结果 Excel？
- 先确认已经执行了"开始研判"。如果研判有报错，把 AI 给出的中文提示发给我们或查看上面的解决方式。
- 若提示"文件可能已被 Excel 打开占用"，请先关闭已打开的旧结果文件再重试。

### Q6：结果想给同事看，怎么做？
- 直接分享 **清单路径下的初筛结果 Excel**（多个地市的清单各占一个工作表）和 **标书路径下的研判报告 md** 即可。
- 每个工作日重新研判时，产物文件名带时间戳（如 `2026-08-05_1530`），不会覆盖同一天之前的版本。

### Q7：项目看起来和我公司相关，但初筛没标"相关"？
- 相关性只是**参考标记**，不会再过滤掉任何项目（防止漏项目）。AI 会把"待确认"的项目列出来问你，你点头就继续研判，不影响。

### Q8：每次都要重新告诉 AI 路径吗？
- 是的。三个路径由用户每次研判时提供，不落盘记忆（安全且灵活：想换文件夹随时换）。如果路径很长，建议复制粘贴。

### Q9：每天新增的公告怎么处理？已研判的会重复出现吗？
- 不会。以**公告链接为唯一键**记录每条记录的 初筛状态/研判状态：已初筛自动跳过初筛，已研判自动跳过研判。每天直接说**"开始研判"**，AI 只处理新增记录，并告诉你"新增几条、跳过几条"。

### Q10：想把之前跳过的项目重新看一遍？
- 说**"全部重新初筛"**或**"重新研判全部项目"**，技能会先把所有记录重置为"新增待初筛"，再按全部新增重新处理（默认不自动重置，需要你明确说）。

### Q11：采集时提示"主文件被 Excel 打开"？
- 说明主文件（`{地市}政府采购公告.xlsx`）正在 Excel 里打开。先关闭 Excel，再让 AI 重新执行采集即可。

---

## 八、注意事项

- 研判结果仅供参考，重要决定请结合实际情况人工复核。
- 每天新增的投标机会，只需要重新放入新的清单和标书（或说"采集"更新主文件），再次说"开始研判"即可。
- 不要改动 `SKILL.md`、`scripts/` 和 `references/` 文件夹里的文件，否则可能无法使用。
