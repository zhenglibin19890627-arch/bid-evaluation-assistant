# -*- coding: utf-8 -*-
"""
excel_tool.py —— Excel 读取、初筛过滤、初筛结果输出、项目类型选择

功能：
1. 读取投标机会清单 Excel（.xlsx），识别字段（多清单合并 + 表头行自动探测）；
2. 初筛：公告 5 天内 + 公告类型合法 + 项目类型命中 + 与公司业务相关（同时满足）；
3. 输出「初筛结果」Excel（{时间}_初筛结果.xlsx，各清单一个 sheet，放清单路径）；
4. 项目类型：20 大类 + 52 子类模板为脚本内置常量（P3 起不随业务路径），用户每次选择的结果
   写入「清单路径/项目类型选择.json」；
5. 提供初筛汇总 markdown 表格生成函数（研判报告 md 用）。

说明：
- 仅支持 .xlsx；若检测到 .xls 或 .csv，给出中文提示"请另存为 .xlsx 格式后重试"。
- 项目类型范围由「清单路径/项目类型选择.json」配置（用户在初筛前确认）；配置缺失或
  无启用类型时自动跳过类型过滤。
- 相关性为软标记：命中"相关"，未命中或资料不齐"待确认"，一律不剔除。
"""

from __future__ import annotations

import json
import os
import re
from datetime import datetime, timedelta

import pandas as pd

# 项目类型模板 v3.2（2026-08-14 重构：通用视角，按采购标的形态组织 20 大类，无兜底全覆盖）
# 设计原则：
#   1. 通用性：任何公司都按"我能提供什么"选用大类，不绑定特定公司业务；
#   2. 全覆盖：子类关键词 → 电子卖场公告规则 → 采购人行业域 → 大类语义词 → 标题形态兜底 五级匹配，100% 命中，无"未分类"；
#   3. 互斥性：子类关键词尽量不重叠，避免一个项目命中多个大类；
#   4. 数据驱动：关键词基于近30天 18,135 条真实公告迭代校准（验证覆盖率 100%）。
# 每次研判由 AI 展示大类让用户选择，再对每个大类选择子类（默认全部子类），结果写入清单路径/项目类型选择.json
PROJECT_TYPES_TEMPLATE = [{"名称": "办公设备与家具", "子类": [{"名称": "计算机与显示设备", "关键词": ["计算机", "台式机", "笔记本", "笔记本电脑", "电脑", "一体机", "工作站", "服务器", "显示屏", "显示器", "大屏", "LED屏", "LED", "投影仪", "投影机", "触控一体机", "电子白板", "固态硬盘", "存储设备", "云终端", "瘦客户机", "电子类产品", "数码", "色选机"]}, {"名称": "打印复印与耗材", "关键词": ["打印机", "复印机", "扫描仪", "打印耗材", "硒鼓", "墨盒", "打印纸", "复印纸", "办公耗材", "速印机", "装订机", "印刷耗材", "用纸", "纸张"]}, {"名称": "办公家具与用品", "关键词": ["办公家具", "办公桌", "办公椅", "会议桌", "文件柜", "档案柜", "密集架", "屏风", "沙发", "茶几", "讲台", "课桌椅", "课桌", "办公用品", "文具", "黑板", "白板", "订书机", "档案盒", "中性笔", "家具采购", "家具", "桌椅", "座椅", "椅子", "写字台", "慰问品"]}, {"名称": "宿舍与生活家具", "关键词": ["床铺", "宿舍床", "学生床", "公寓床", "学生公寓", "储物柜", "更衣柜", "窗帘", "床上用品", "床品", "被褥", "被套", "床垫", "折叠床", "衣柜", "洗衣池", "晾衣", "宿舍", "生活设施", "校服"]}, {"名称": "音视频会议设备", "关键词": ["音响", "功放", "麦克风", "话筒", "音箱", "调音台", "视频会议", "会议系统", "摄像机", "摄像头", "录播", "广播系统", "校园广播", "LED显示屏", "拼接屏", "矩阵", "音频", "灯光系统"]}]}, {"名称": "家电机电与通用设备", "子类": [{"名称": "空调暖通设备", "关键词": ["空调", "中央空调", "多联机", "风管机", "空气能", "热泵", "新风", "通风", "取暖器", "开水器", "直饮机", "饮水机", "冷柜", "除湿机", "暖通"]}, {"名称": "家用电器", "关键词": ["洗衣机", "冰箱", "冰柜", "电视机", "热水器", "电热水壶", "微波炉", "消毒柜", "电风扇", "吸尘器", "干衣机", "家电", "电器设备", "电器采购", "电器"]}, {"名称": "电梯与机电设备", "关键词": ["电梯", "扶梯", "升降机", "发电机", "柴油发电机", "机电设备", "水泵", "变压器", "配电柜", "起重", "塔吊", "锅炉", "压力容器", "叉车", "空压机", "特种设备", "林机设备", "农机"]}, {"名称": "厨房设备", "关键词": ["厨房设备", "灶具", "油烟机", "蒸柜", "蒸饭车", "消毒柜", "洗碗机", "冰柜", "保鲜柜", "厨具", "烹饪设备", "燃气灶", "烤箱", "厨房"]}, {"名称": "劳保与通用物资", "关键词": ["劳保", "劳保用品", "工作服", "工装", "安全帽", "防护服", "手套", "雨鞋", "雨具", "帐篷", "棉被", "救灾物资", "防汛物资", "应急物资", "标识标牌", "宣传栏", "告示牌", "服装", "制服", "被装", "辅警服装", "鞋帽"]}]}, {"名称": "教学装备", "子类": [{"名称": "教学仪器与实验设备", "关键词": ["教学仪器", "教学设备", "实验设备", "实验仪器", "实验室", "理化生", "专用教室", "功能教室", "仪器设备", "实验器材", "化学试剂", "显微镜", "天平", "模型", "创新教室", "数字电子", "电子控制", "实验功能室"]}, {"名称": "实训与教具设备", "关键词": ["实训", "实训设备", "实训室", "实训基地", "虚拟仿真", "创客", "3D打印", "数控", "车床", "钳工", "焊接", "汽修", "教具", "玩具", "学具", "感统", "蒙氏", "积木", "专业群建设", "实训基地"]}, {"名称": "教育信息化设备", "关键词": ["智慧黑板", "交互平板", "教学平板", "电子班牌", "多媒体教室", "录播教室", "录播系统", "智慧课堂", "校园网络", "机房改造", "教学电脑", "智慧体育", "智能体育", "一体机教学", "班班通", "护眼灯", "教室灯光", "人脸识别", "考勤系统", "闸机", "电子校徽", "智慧安防", "AR预警", "智能引导", "电子类产品", "校园设备", "教育装备", "教学用品", "智能批阅", "数字教育"]}, {"名称": "体育器材与健身设施", "关键词": ["体育器材", "健身器材", "运动器材", "篮球架", "足球门", "乒乓球台", "羽毛球", "塑胶跑道", "人造草坪", "操场", "运动场", "体测", "体质测试", "单双杠", "攀爬", "游乐设施", "滑梯", "健身设施", "体育场地"]}, {"名称": "校园后勤设施", "关键词": ["宿舍楼", "接送系统", "护学岗", "防冲撞", "升降柱", "安防系统", "校园监控", "一键报警", "消防器材", "灭火器", "消毒灯", "紫外线灯", "午休", "躺睡", "课桌椅", "宿舍家具", "接送区", "等候区", "校园设施", "食堂设备", "图书"]}]}, {"名称": "教育培训服务", "子类": [{"名称": "培训与课程服务", "关键词": ["培训", "研修", "轮训", "课程", "技能培训", "职业培训", "继续教育", "师资", "培养", "培育", "素质提升", "研学", "产教融合", "技能课程", "职教", "教学能力", "教育质量提升", "教师培训", "校长", "班主任", "辅导员"]}, {"名称": "人才与人力资源服务", "关键词": ["人才", "人才培育", "工程师", "人才队伍", "招聘", "引才", "高层次人才", "劳动力", "人力资源"]}, {"名称": "疗休养与职工福利", "关键词": ["疗休养", "休养", "职工疗", "劳模", "一线职工", "职工福利"]}]}, {"名称": "医疗设备与医用耗材", "子类": [{"名称": "影像诊断设备", "关键词": ["磁共振", "CT", "DR", "DSA", "X射线", "X光机", "C臂", "彩超", "B超", "超声", "骨密度", "内窥镜", "内镜", "腔镜", "胃肠镜", "支气管镜", "眼底", "OCT", "放射", "影像设备", "钼靶", "牙科CT", "液质联用", "质谱", "图谱仪"]}, {"名称": "治疗急救设备", "关键词": ["呼吸机", "监护仪", "除颤仪", "血透", "透析", "麻醉机", "手术床", "无影灯", "注射泵", "输液泵", "心电图机", "胎心", "复苏", "高压氧", "治疗仪", "理疗仪", "康复设备", "激光治疗", "碎石", "急救设备", "救护车", "病床", "电动病床", "普通病床", "床单元", "热灌注", "抗衰", "光电抗衰", "直线加速器"]}, {"名称": "检验与实验室设备", "关键词": ["检验设备", "生化分析", "血球分析", "凝血", "尿液分析", "免疫分析", "PCR", "基因", "质谱", "离心机", "培养箱", "显微镜", "病理", "流式", "医用冰箱", "试剂冷藏", "检验科", "分析仪", "测定仪", "血气分析", "血细胞分析", "尿沉渣", "病毒载量", "血液分析"]}, {"名称": "药品试剂与医用耗材", "关键词": ["药品", "试剂", "医用耗材", "耗材", "医用气体", "液氧", "医用氧", "氧气", "缝合", "穿刺", "导管", "敷料", "绷带", "手套", "口罩", "防护服", "消毒液", "消毒剂", "清洗液", "营养制剂", "特医食品", "配方食品", "中药饮片", "颗粒剂", "医用纯水", "纯水设备"]}]}, {"名称": "车辆船艇与运输", "子类": [{"名称": "公务与专用车辆", "关键词": ["车辆", "公务用车", "执法执勤车", "警车", "巡逻车", "消防车", "救护车", "环卫车", "洒水车", "垃圾车", "校车", "通勤车", "汽车", "新能源车", "电动车", "摩托车", "皮卡", "面包车", "献血车"]}, {"名称": "船艇采购", "关键词": ["执法船", "船舶", "船艇", "执法艇", "巡逻艇", "公务艇", "渡船", "渔船", "快艇", "水上"]}, {"名称": "运输物流与燃油", "关键词": ["运输服务", "物流", "快递", "搬运", "装卸", "货运", "燃油", "柴油", "汽油", "加油服务", "班车", "包车", "租车"]}]}, {"名称": "建筑工程与装修改造", "子类": [{"名称": "土建施工", "关键词": ["土建", "建筑施工", "建设工程", "新建", "迁建", "扩建", "改建", "主体工程", "框架结构", "砌体", "地基", "桩基", "主体施工", "钢结构", "混凝土", "施工项目", "施工工程"]}, {"名称": "改造修缮工程", "关键词": ["改造工程", "改造", "修缮", "维修", "翻新", "加固", "提升改造", "维修改造", "整治", "维修工程", "防水", "屋面", "老旧小区", "功能性改造", "微改造", "提升工程", "提升建设", "古民居", "修复", "善居"]}, {"名称": "装修装饰与布展", "关键词": ["装修", "装饰", "精装修", "布展", "展陈", "展厅", "展区", "墙面粉刷", "涂料", "门窗", "吊顶", "地板", "幕墙", "标识标牌", "文化墙", "驿站"]}, {"名称": "拆除搬迁与场地", "关键词": ["拆除", "拆迁", "搬迁", "清运", "场地平整", "土方", "渣土", "临建"]}]}, {"名称": "市政与园林水利工程", "子类": [{"名称": "市政道路管网", "关键词": ["市政", "道路", "公路", "桥梁", "隧道", "管网", "管道", "供水管网", "排水管网", "污水管网", "雨污", "路灯", "亮化", "信号灯", "护栏", "人行道", "候车亭", "停车场", "交通设施", "标线", "电警", "电子警察", "交通管理"]}, {"名称": "园林绿化景观", "关键词": ["绿化", "园林", "景观", "花卉", "鲜花", "绿植", "绿道", "公园", "广场", "草坪", "养护", "苗木", "大树", "修剪", "市容", "美丽宜居", "和美乡村"]}, {"名称": "水利防洪防汛", "关键词": ["水利", "水库", "河道", "堤防", "防汛", "排涝", "灌溉", "山塘", "灌区", "水文", "泵站", "水闸", "山洪", "水毁", "引水", "蓄水", "清淤", "疏浚", "水旱灾害", "水源地", "供水", "节水", "水效", "用水"]}]}, {"名称": "电力电气与新能源工程", "子类": [{"名称": "电力配电工程", "关键词": ["电力工程", "配电", "供电", "用电", "通电", "输电", "变电站", "配电房", "电缆", "电气安装", "电气工程", "强电", "电表", "增容", "电力设施"]}, {"名称": "光伏与新能源", "关键词": ["光伏", "太阳能", "充电桩", "充电站", "储能", "风能", "风电", "新能源", "电池"]}, {"名称": "路灯亮化工程", "关键词": ["路灯", "亮化", "照明", "景观灯", "庭院灯", "LED路灯", "灯杆", "城市照明"]}]}, {"名称": "信息化系统与集成", "子类": [{"名称": "系统集成与弱电工程", "关键词": ["系统集成", "弱电", "综合布线", "智能化工程", "楼宇自控", "安防监控", "视频监控", "监控系统", "门禁", "道闸", "报警", "机房建设", "机房工程", "UPS", "综合安防", "物联网", "智慧园区", "智慧社区", "智慧楼宇", "数字化工程", "信息化工程"]}, {"名称": "软件开发与平台", "关键词": ["软件开发", "软件", "系统开发", "平台建设", "APP", "小程序", "公众号", "门户网站", "网站", "OA", "ERP", "CRM", "数据库", "管理系统", "业务系统", "数字化", "信息化", "一网通办", "电子政务", "政务云", "城市大脑", "数据中台", "数据平台", "智慧城市", "数字乡村", "数字孪生", "数智", "应用系统", "智能系统", "智慧系统", "信息化项目", "数字化项目", "医保", "智能监管", "智能审核", "一张图", "时空信息"]}, {"名称": "网络通信", "关键词": ["网络", "局域网", "专线", "宽带", "5G", "光纤", "交换机", "路由器", "AP", "无线网络", "通信", "链路", "物联网卡", "短信", "链路租用", "城域网"]}, {"名称": "数据中心与机房", "关键词": ["数据中心", "机房", "云服务", "云计算", "服务器", "存储", "虚拟化", "超融合", "灾备", "备份"]}, {"名称": "AI与新兴技术", "关键词": ["人工智能", "AI", "大模型", "智能体", "机器人", "无人机", "低空", "遥感", "卫星", "数字人", "语音识别", "图像识别", "OCR", "算法", "深度学习", "元宇宙"]}, {"名称": "信创国产化", "关键词": ["信创", "国产化", "自主可控", "麒麟", "统信", "鲲鹏", "飞腾", "龙芯", "国产芯片", "鸿蒙", "中间件"]}]}, {"名称": "网络安全与数据安全", "子类": [{"名称": "网络安全防护", "关键词": ["网络安全", "防火墙", "入侵检测", "IPS", "WAF", "防病毒", "杀毒", "终端安全", "态势感知", "安全运营", "网络防护", "安全加固", "堡垒机"]}, {"名称": "等保测评与密码", "关键词": ["等保", "等级保护", "测评", "密评", "商用密码", "密码改造", "密码应用", "密钥", "加密"]}, {"名称": "数据安全治理", "关键词": ["数据安全", "数据治理", "隐私计算", "脱敏", "防泄漏", "DLP", "容灾", "灾备", "日志审计"]}]}, {"名称": "信息技术服务", "子类": [{"名称": "运维维保服务", "关键词": ["运维", "维保", "维护", "保修", "保养", "巡检", "值守", "运行维护", "维护保障", "驻场", "系统维护", "设备维护", "技术支持", "客服外包", "打印输出"]}, {"名称": "IT外包与人员服务", "关键词": ["外包", "IT外包", "劳务", "派遣", "驻点", "服务外包", "人力", "工程师", "外包服务", "辅助人员"]}, {"名称": "数据与云服务", "关键词": ["云服务", "数据服务", "数据采集", "数据分析", "数据录入", "数据加工", "标注", "清洗", "地图服务", "GIS", "地理信息"]}, {"名称": "档案数字化服务", "关键词": ["档案数字化", "数字化加工", "档案整理", "扫描", "翻拍", "电子卷宗", "著录", "归档", "缩微", "档案"]}]}, {"名称": "后勤物业与餐饮", "子类": [{"名称": "物业服务", "关键词": ["物业", "物业管理", "物业化", "秩序维护", "门卫", "巡逻", "宿管", "宿舍管理"]}, {"名称": "保洁与绿化养护", "关键词": ["保洁", "清扫", "清洗", "消杀", "除四害", "绿化养护", "绿化管养", "养护", "除雪", "开荒保洁"]}, {"名称": "安保服务", "关键词": ["保安", "安保", "安全保卫", "安保守卫", "治安", "执勤", "押运", "安全管家"]}, {"名称": "食堂餐饮与食材", "关键词": ["食堂", "餐饮", "食材", "粮油", "米面", "蔬菜", "肉类", "配送", "营养餐", "学生奶", "食品", "伙食", "餐具", "就餐", "膳食", "副食品", "食堂管理", "食堂服务", "食堂托管", "点心", "牛奶"]}, {"名称": "劳务派遣", "关键词": ["劳务", "派遣", "用工", "临时用工", "工勤", "辅助性岗位", "劳务服务", "劳动力", "社会化服务"]}, {"名称": "房屋租赁与场地", "关键词": ["租赁", "租用", "租房", "场地租赁", "房屋", "办公用房", "宿舍租赁", "代管", "运营管理"]}]}, {"名称": "医疗健康服务", "子类": [{"名称": "体检与康复服务", "关键词": ["体检", "健康体检", "康复", "康复治疗", "理疗", "推拿", "针灸", "运动康复", "健康管理"]}, {"名称": "口腔与眼科服务", "关键词": ["口腔", "牙科", "洗牙", "种植牙", "眼科", "视力", "近视", "验光"]}, {"名称": "护理陪护服务", "关键词": ["护理", "陪护", "护工", "护士", "照护", "看护", "居家护理"]}, {"名称": "医废处理与洗涤", "关键词": ["医废", "医疗废物", "洗涤", "被服", "消毒供应", "清洗消毒", "布草", "洗涤服务"]}, {"名称": "公共卫生防疫", "关键词": ["疾控", "防疫", "疫苗接种", "病媒", "消杀", "爱国卫生", "公共卫生", "疟疾", "登革热", "蚊虫"]}]}, {"名称": "安全消防与应急", "子类": [{"名称": "消防工程与器材", "关键词": ["消防", "消控", "灭火器", "烟感", "防火", "应消", "消防器材", "消防工程", "消防设施", "消防设备", "微型消防站"]}, {"名称": "应急与救援", "关键词": ["应急", "救援", "防汛", "救灾", "防灾", "避灾", "安全生产", "隐患排查", "应急指挥", "应急广播", "应急小单元", "应急救援分队", "应急保障", "应急器材", "应急装备"]}, {"名称": "安防监控设备", "关键词": ["安防", "监控", "视频监控", "雪亮工程", "电子围栏", "天网", "执法仪", "勤务指挥", "应急消防管理站", "提能升级"]}]}, {"名称": "专业服务", "子类": [{"名称": "监理与招标代理", "关键词": ["监理", "招标代理", "代理服务", "造价咨询", "全过程咨询", "项目管理", "代建"]}, {"名称": "规划设计与造价", "关键词": ["设计", "方案设计", "施工图", "规划设计", "规划编制", "详规", "修规", "可研", "可行性研究", "造价", "预算编制", "结算审核", "勘察", "测绘", "测量", "勘测定界", "效果图", "建模", "规划方案", "控制性详细规划", "土地征收", "成片开发", "多田套合", "实施方案"]}, {"名称": "检验检测认证", "关键词": ["检测", "检验", "检测服务", "认证", "鉴定", "校准", "检定", "评审", "抽查", "抽检", "计量", "质检", "检测评估", "评估", "安全评估", "风险评估", "环境影响评价", "环评", "安全风险", "咨询服务"]}, {"名称": "财务审计与评估", "关键词": ["财务", "审计", "会计", "决算", "协审", "税务", "资产评估", "绩效评价", "绩效评估", "清算", "保险", "责任险", "巨灾"]}, {"名称": "法律与档案服务", "关键词": ["法律", "律师", "法律服务", "调解", "仲裁", "公证", "诉讼", "档案", "档案服务", "档案管理", "知识产权", "专利", "商标"]}, {"名称": "调查普查与统计", "关键词": ["调查", "普查", "统计", "监测", "抽样", "问卷", "数据统计", "人口普查", "经济普查", "农业普查", "满意度", "第三方", "督查", "评估"]}, {"名称": "科研与课题研究", "关键词": ["研究", "课题", "科研", "咨询研究", "路径研究", "机制研究", "试点研究", "发展战略", "规划研究", "研究服务"]}]}, {"名称": "文化宣传与文体活动", "子类": [{"名称": "广告印刷出版", "关键词": ["广告", "印刷", "喷绘", "印刷品", "出版物", "图书", "教材", "教辅", "作业本", "期刊", "报纸", "宣传册", "海报", "标识", "标牌", "文创", "视频制作", "拍摄", "专题片", "宣传片", "教科书", "学生用书"]}, {"名称": "媒体宣传推广", "关键词": ["媒体", "宣传", "推广", "融媒体", "新闻", "报道", "公众号运营", "新媒体", "直播", "代言", "品牌", "形象升级"]}, {"名称": "文化活动与演出", "关键词": ["演出", "文艺", "晚会", "艺术节", "文化节", "典礼", "仪式", "展览", "展会", "会展", "博览会", "论坛", "峰会", "活动", "比赛", "赛事", "运动会", "汇演", "歌咏", "演讲", "歌王", "争霸赛", "挑战赛", "巡回赛", "围棋", "轮滑", "桨板", "精武", "综合格斗", "文化礼堂", "传统文化", "村落保护"]}, {"名称": "体育赛事活动", "关键词": ["赛事", "比赛", "竞赛", "联赛", "锦标赛", "马拉松", "越野", "龙舟", "运动会", "体育赛", "公开赛", "俱乐部杯"]}, {"名称": "场馆运营与文旅", "关键词": ["运营", "场馆", "博物馆", "图书馆", "文化馆", "科技馆", "旅游", "景区", "民宿", "酒店", "研学旅行", "导游", "文旅", "大剧院", "会议服务", "对接会"]}]}, {"名称": "农林牧渔与生态环保", "子类": [{"名称": "农业种植与农资", "关键词": ["农业", "农田", "高标准农田", "粮食", "水稻", "小麦", "玉米", "种子", "化肥", "农药", "农资", "植保", "统防统治", "耕地", "农作物", "大棚", "滴灌", "水肥", "制种", "种质资源", "食用菌", "稻鱼", "杨梅", "油茶", "山茶油", "畲药", "药材", "茶产业"]}, {"名称": "林业与生态修复", "关键词": ["林业", "森林", "造林", "抚育", "防火", "护林", "古树", "林场", "松材线虫", "疫木", "湿地", "生态修复", "生态治理", "生物多样性", "退耕", "种草", "封山", "生态定位", "公益林", "良种基地", "注干剂"]}, {"名称": "畜牧渔业", "关键词": ["畜牧", "养殖", "饲料", "防疫", "检疫", "屠宰", "渔业", "渔政", "增殖放流", "鱼苗", "水生", "捕捞", "增殖", "病死畜禽", "无害化"]}, {"名称": "环境治理与监测", "关键词": ["环保", "环境治理", "环境监测", "污水", "废水", "废气", "大气", "土壤", "固废", "危废", "污染", "排污", "水质监测", "空气监测", "噪声", "黑臭水体", "五水共治", "水生态", "环境健康", "宁静小区", "生态环境"]}, {"名称": "环卫与垃圾处理", "关键词": ["环卫", "垃圾", "垃圾分类", "清运", "转运", "压缩站", "焚烧", "填埋", "粪便", "公厕"]}, {"名称": "地质气象", "关键词": ["地质", "气象", "地质灾害", "滑坡", "矿山", "尾矿", "勘查", "人工影响天气", "雨量", "水文气象", "自动站", "测雨雷达", "生态流量"]}]}, {"名称": "民政福利与养老", "子类": [{"名称": "养老服务", "关键词": ["养老", "居家养老", "养老服务", "养老院", "敬老院", "老年", "助餐", "适老化", "医养", "善居工程"]}, {"名称": "福利救助", "关键词": ["福利", "救助", "慈善", "帮扶", "残疾人", "孤儿", "困难群众", "临时救助", "低保"]}, {"名称": "殡葬服务", "关键词": ["殡葬", "公墓", "殡仪", "火化", "墓地", "骨灰"]}]}, {"名称": "电子卖场类", "子类": [{"名称": "电子卖场公告", "关键词": ["电子卖场"]}, {"名称": "竞价与意向公告", "关键词": ["竞价", "采购意向", "意向", "公开征集", "反拍", "询价", "报价", "框架协议"]}]}]

# 大类级语义词（子类关键词未命中时按大类宽词归类；不含'设备/服务/工程'等全通用词）
BIG_SEMANTIC_WORDS = {"办公设备与家具": ["办公", "办公设备", "电脑", "计算机", "打印机", "复印", "打印", "家具", "文具", "办公用品", "办公桌椅", "课桌椅", "耗材", "纸张", "簿册", "书册"], "家电机电与通用设备": ["空调", "电梯", "家电", "电器", "机电", "发电机", "锅炉", "厨房设备", "劳保", "工作服", "制服", "开水器", "饮水机", "厨具"], "教学装备": ["学校", "校园", "教学", "教室", "学生", "教育", "课堂", "课桌", "器材", "体育", "操场", "幼儿园", "中小学", "学院", "大学", "职教", "实训", "图书"], "教育培训服务": ["培训", "研修", "研学", "课程", "师资", "培养", "培育", "教师", "班主任", "疗休养", "休养", "劳模", "人才", "教育质量"], "医疗设备与医用耗材": ["医院", "医疗", "医用", "器械", "卫生院", "妇幼", "疾控", "卫生", "手术", "麻醉", "器械", "耗材", "血站", "试剂", "药品", "医共体", "口腔", "眼科"], "车辆船艇与运输": ["车辆", "用车", "汽车", "货车", "运输", "物流", "船", "艇", "货运", "班车", "燃油", "加油"], "建筑工程与装修改造": ["工程", "施工", "建设", "改造", "修缮", "装修", "装饰", "拆除", "搬迁", "加固", "翻新", "维修", "建设"], "市政与园林水利工程": ["市政", "道路", "管网", "管道", "桥梁", "隧道", "路灯", "绿化", "园林", "景观", "水利", "河道", "水库", "堤防", "防汛", "灌溉", "山塘", "排水", "供水", "污水", "管廊"], "电力电气与新能源工程": ["电力", "配电", "供电", "光伏", "充电", "储能", "电气", "电缆", "变电站", "亮化", "照明"], "信息化系统与集成": ["信息化", "数字化", "智能化", "软件", "系统", "平台", "网络", "数据", "智能", "智慧", "机房", "监控", "集成", "弱电", "物联网", "APP", "小程序", "云计算", "数字"], "网络安全与数据安全": ["网络安全", "等保", "密码", "安全防护", "防火墙", "数据安全", "攻防", "渗透"], "信息技术服务": ["运维", "维保", "维护", "外包", "驻场", "巡检", "值守", "档案", "数字化加工", "扫描"], "后勤物业与餐饮": ["物业", "保洁", "食堂", "餐饮", "食材", "粮油", "安保", "保安", "劳务", "派遣", "后勤", "宿管", "保安", "伙食", "营养餐", "派遣"], "医疗健康服务": ["体检", "康复", "护理", "陪护", "防疫", "公卫", "医废", "洗涤", "消杀", "健康"], "安全消防与应急": ["消防", "应急", "安防", "救援", "灭火", "安全生产", "防火", "防灾", "避险"], "专业服务": ["咨询", "监理", "设计", "规划", "检测", "检验", "审计", "评估", "调查", "研究", "招标代理", "法律服务", "造价", "普查", "认证", "鉴定", "绩效", "编制", "方案"], "文化宣传与文体活动": ["文化", "宣传", "活动", "演出", "赛事", "比赛", "广告", "印刷", "出版", "图书", "媒体", "推广", "体育赛", "会展", "文旅", "旅游", "景区", "演出", "文艺"], "农林牧渔与生态环保": ["农业", "林业", "生态", "环保", "环境", "渔业", "垃圾", "地质", "气象", "森林", "农田", "养殖", "无害化", "环卫", "污水治理", "大气"], "民政福利与养老": ["养老", "福利", "救助", "殡葬", "民政", "公墓", "慈善", "残疾人", "困难群众"], "电子卖场类": ["电子卖场", "竞价", "意向", "框架协议", "询价", "反拍"]}

# 采购人行业域（采购人关键词 → 大类；项目名未命中时按采购人归类）
SECTOR_BUYER_KWS = [("教学装备", ["教育局", "学校", "中学", "小学", "幼儿园", "学院", "大学", "职教", "教育", "校园", "托育", "学区", "技师学院", "教科所"]), ("医疗设备与医用耗材", ["医院", "卫生院", "医共体", "疾控", "卫生健康", "人民医院", "血站", "妇幼保健", "血库", "医疗中心", "疾病预防控制"]), ("车辆船艇与运输", ["交通局", "交通运输", "公路"]), ("市政与园林水利工程", ["水利局", "水利", "住建局", "市政", "综合行政执法局", "水设施", "河道保护"]), ("安全消防与应急", ["公安", "派出所", "交警", "消防", "应急管理局", "安监局", "监狱", "法院", "检察", "司法"]), ("专业服务", ["司法局", "财政局", "审计局", "统计局", "自然资源和规划局", "税务局", "发展和改革局", "发改局", "经济和信息化局", "社会保险", "公积金", "社保"]), ("民政福利与养老", ["民政局", "退役军人事务局", "残联", "红十字会", "总工会", "老干部"]), ("文化宣传与文体活动", ["文化和广电旅游体育局", "宣传部", "文广旅体", "文旅", "融媒体", "传媒集团", "剧团", "文化馆", "图书馆", "越剧", "电视"]), ("农林牧渔与生态环保", ["农业农村局", "生态环境局", "林业局", "自然资源和规划局", "生态林业", "林场", "海洋经济发展局", "自然保护区"])]

# 标题形态兜底（通用词 → 大类；以上均未命中时按标题形态归类，保证 100% 命中无兜底）
FORM_FALLBACK = [(["工程", "施工", "建设", "改造", "修缮", "装修", "土建", "整治", "基础设施", "综合体", "改建", "新建"], "建筑工程与装修改造"), (["设备", "器材", "仪器", "器械", "物资", "用具", "工具", "装备", "机", "系统采购", "设施采购"], "家电机电与通用设备"), (["平台", "系统", "软件", "网络", "信息化", "数字化", "智能化", "数据", "机房", "应用", "智管", "智治", "模块", "热线", "送达", "协同", "事件库", "在线", "举报"], "信息化系统与集成"), (["服务", "运营", "托管", "管理", "保障", "运维", "研究", "谋划", "对策", "建议", "咨询", "评估", "调查", "普查", "调研", "编制", "编纂", "协议", "合作"], "专业服务"), (["车辆", "用车", "船", "运输", "物流", "燃油", "自行车", "执法车", "执法执勤"], "车辆船艇与运输"), (["文化", "宣传", "活动", "赛事", "比赛", "广告", "印刷", "图书", "越剧", "演出", "电影", "交流", "推介", "传播", "氛围", "专栏", "栏目", "非遗", "遗址", "创业大赛", "嘉年华", "柔力球", "彩票", "赛务", "创排", "订单班", "培训"], "文化宣传与文体活动"), (["绿化", "园林", "市政", "道路", "管网", "水利", "河道", "水库", "航道", "疏导", "交安", "上学路", "保畅"], "市政与园林水利工程"), (["农业", "林业", "渔业", "生态", "环保", "垃圾", "环境", "互花米草", "白蚁", "保种", "蚕桑", "稻鳖", "茶园", "菜篮子", "共富", "防控", "图斑", "富营养化", "复垦", "土壤", "水域"], "农林牧渔与生态环保"), (["养老", "福利", "救助", "殡葬", "陵园", "慰问", "补贴", "爱心卡", "青年之家", "商品房", "房租", "幸福家园"], "民政福利与养老"), (["医疗", "医院", "卫生", "健康", "血液", "血袋", "血浆", "疫苗", "冷库", "中药", "消毒"], "医疗健康服务"), (["消防", "应急", "安防", "警戒", "围墙", "密改"], "安全消防与应急"), (["培训", "研修", "研学", "课程", "工伤", "预防", "技能", "人才"], "教育培训服务"), (["办公", "家具", "打印", "文具", "用品"], "办公设备与家具"), (["电力", "配电", "光伏", "充电", "能源", "线杆"], "电力电气与新能源工程"), (["土地", "测绘", "地形图", "权属", "延包", "发证", "变更"], "专业服务"), (["冻猪肉", "储备", "猪肉"], "后勤物业与餐饮"), (["射击", "攀岩", "射箭", "提档升级", "国际日"], "文化宣传与文体活动"), (["光谱仪", "电镜", "光谱", "检测仪", "测定仪"], "专业服务"), (["备件", "配件"], "家电机电与通用设备"), (["地价"], "专业服务"), (["矿尾水", "尾矿", "尾水"], "农林牧渔与生态环保"), (["乡宿", "民宿", "溯溪", "点靓", "精品线"], "文化宣传与文体活动"), (["林草", "有害生物", "迁地保护", "野生植物", "补充林地"], "农林牧渔与生态环保"), (["综合险", "信用险", "资金归集", "存放"], "专业服务"), (["未来社区", "城市更新", "基层治理", "综合治理"], "信息化系统与集成"), (["研发费用", "攻坚", "试点"], "专业服务"), (["青年入乡", "实践基地"], "文化宣传与文体活动"), (["项目"], "专业服务")]

# 旧 18 类名称 → 新 20 大类映射（P3.1 修复：旧格式 项目类型选择.json 兼容，
# 旧类名映射到新大类后走正常大类校验；无法映射的名称给出中文警告，不静默丢弃）
OLD_TYPE_NAME_MAP = {
    "教育信息化": "教学装备",
    "教育培训与设备": "教学装备",
    "教学仪器设备": "教学装备",
    "办公设备": "办公设备与家具",
    "办公设备与家具用品": "办公设备与家具",
    "家具办公用品": "办公设备与家具",
    "医疗信息化": "信息化系统与集成",
    "医疗器械": "医疗设备与医用耗材",
    "医疗健康": "医疗设备与医用耗材",
    "政务信息化": "信息化系统与集成",
    "政务与安防信息化": "信息化系统与集成",
    "智慧园区社区": "信息化系统与集成",
    "智慧园区/社区": "信息化系统与集成",
    "公安安防信息化": "信息化系统与集成",
    "系统集成": "信息化系统与集成",
    "信息化与智能化": "信息化系统与集成",
    "运维服务": "信息技术服务",
    "网络安全服务": "网络安全与数据安全",
    "家电机电设备": "家电机电与通用设备",
    "后勤物业与餐饮": "后勤物业与餐饮",
    "印刷广告服务": "文化宣传与文体活动",
    "文化体育与文旅": "文化宣传与文体活动",
    "建筑装饰工程": "建筑工程与装修改造",
    "市政园林工程": "市政与园林水利工程",
    "工程建设与市政": "建筑工程与装修改造",
    "生态环保与能源": "农林牧渔与生态环保",
    "农林牧渔与水利": "农林牧渔与生态环保",
    "专业技术服务": "专业服务",
    "电子卖场类": "电子卖场类",
    "安全消防与应急": "安全消防与应急",
    "车辆船艇与运输": "车辆船艇与运输",
    "土地测绘与自然资源": "专业服务",
    "民政福利与养老": "民政福利与养老",
    "医疗健康服务": "医疗健康服务",
    "电力电气与新能源工程": "电力电气与新能源工程",
}

# 项目类型选择结果文件名（写在清单路径下）
PROJECT_TYPES_SELECTION_FILE = "项目类型选择.json"

# 时间范围选择结果文件名（写在清单路径下；每次初筛可不同，未配置/不限时不强制限定）
TIME_RANGE_FILE = "时间范围选择.json"

# 合法公告类型（需求确认）
VALID_ANNOUNCE_TYPES = ("采购项目公告", "电子卖场公告")

# 需求字段清单
REQUIRED_COLUMNS = [
    "公告时间",
    "公告类型",
    "采购项目名称",
]

# 常用字段全名（用于判断列是否存在）
ALL_COLUMNS = [
    "公告时间",
    "公告类型",
    "所在区县",
    "采购人",
    "采购项目名称",
    "预算金额(元)",
    "中标金额(元)",
    "中标单位",
    "代理机构",
    "地址链接URL",
]


class ExcelToolError(Exception):
    """Excel 处理过程中的中文可读异常。"""


def check_format_supported(path: str) -> str:
    """检查文件格式，返回扩展名或抛出中文提示。"""
    ext = os.path.splitext(path)[1].lower()
    if ext == ".xlsx":
        return ext
    if ext in (".xls", ".xlsm", ".csv"):
        raise ExcelToolError(
            "检测到文件格式为「%s」。请先在 Excel 中打开该文件，"
            "点击「文件 → 另存为 → Excel 工作簿(.xlsx)」保存后，再放回清单路径重试。" % ext
        )
    raise ExcelToolError("无法识别的文件格式「%s」，请提供 .xlsx 格式的投标机会清单。" % ext)


def _parse_date(value):
    """将公告时间解析为 datetime，兼容多种格式。"""
    if value is None or (isinstance(value, float) and pd.isna(value)):
        return None
    if isinstance(value, datetime):
        return value
    if isinstance(value, pd.Timestamp):
        return value.to_pydatetime()
    if isinstance(value, (int, float)):
        # 可能是 Excel 序列日期
        try:
            return pd.Timestamp(value).to_pydatetime()
        except Exception:
            return None
    text = str(value).strip()
    if not text:
        return None
    # 常见中文日期格式
    for fmt in (
        "%Y-%m-%d %H:%M:%S",
        "%Y-%m-%d %H:%M",
        "%Y-%m-%d",
        "%Y/%m/%d",
        "%Y年%m月%d日",
        "%Y/%m/%d %H:%M:%S",
    ):
        try:
            return datetime.strptime(text, fmt)
        except ValueError:
            continue
    # 兜底：用 pandas 解析
    try:
        return pd.to_datetime(text).to_pydatetime()
    except Exception:
        return None


def load_excel(path: str) -> pd.DataFrame:
    """读取 Excel 清单，返回 DataFrame。"""
    if not os.path.exists(path):
        raise ExcelToolError("未找到投标机会清单文件：%s\n请确认清单路径下存在 .xlsx 清单文件。" % path)

    check_format_supported(path)

    try:
        df = pd.read_excel(path, dtype=str)
    except Exception as e:
        raise ExcelToolError("读取 Excel 失败，请确认文件未被占用且格式正确。详细信息：%s" % e)

    if df.empty:
        raise ExcelToolError("Excel 文件为空，没有可处理的投标机会数据。")

    # 检查必需列
    missing = [c for c in REQUIRED_COLUMNS if c not in df.columns]
    if missing:
        raise ExcelToolError(
            "Excel 缺少以下必需列：%s。\n请确认清单包含：公告时间、公告类型、采购项目名称等列。"
            % "、".join(missing)
        )
    return df


def _detect_header_row(path: str, max_scan: int = 5) -> int:
    """探测表头行：扫描前 max_scan 行，找到含「采购项目名称」的行（1 起）。找不到返回 1。"""
    try:
        head = pd.read_excel(path, header=None, nrows=max_scan, dtype=str)
    except Exception:
        return 1
    for i in range(len(head)):
        row_vals = [str(v) for v in head.iloc[i].tolist() if str(v) and str(v) != "nan"]
        if any("采购项目名称" in v for v in row_vals):
            return i + 1
    return 1


def load_excels_from_dir(list_dir: str) -> dict:
    """
    扫描清单路径下全部 .xlsx 清单并合并（自动探测各文件表头行）。

    多 Sheet 主文件支持（用户单文件工作流：政府采购公告.xlsx 不同地区不同 Sheet）：
    - 文件含多个 Sheet 时逐 Sheet 读取，来源标记为「文件名::Sheet名」；
    - 每个 Sheet 独立探测表头行、独立校验必需列；单个 Sheet 失败不阻塞其他 Sheet。

    返回 {"df": 合并DataFrame(含"来源文件""原始行号"列), "sources": {来源名: 绝对路径},
          "header_rows": {来源名: 表头行号}, "errors": [个别文件的中文错误]}
    - 表头不在第 1 行的文件（如丽水表头在第 3 行）自动适配；
    - 单个文件缺必需列或读取失败：记入 errors，不阻塞其他文件；
    - 全部文件失败或无 .xlsx 时抛出 ExcelToolError（含 .xls/.csv 转换提示）。
    """
    if not os.path.isdir(list_dir):
        raise ExcelToolError("未找到清单路径：%s\n请确认该文件夹存在（放置投标机会清单 Excel 的文件夹）。" % list_dir)
    files = sorted(
        f for f in os.listdir(list_dir)
        if f.lower().endswith(".xlsx") and not f.startswith(("~$", "."))
        # 排除本工具输出文件（初筛结果/历史研判结果/待研判清单视图），避免被当作来源清单重复扫描
        and "初筛结果" not in f and "研判结果" not in f and "待研判清单" not in f
        # 排除采集器高亮版输出文件（{地市}政府采购公告_高亮版.xlsx），防止被当清单读入
        and "高亮版" not in f
    )
    if not files:
        other_formats = [
            f for f in os.listdir(list_dir)
            if f.lower().endswith((".xls", ".csv")) and not f.startswith(("~$", "."))
        ]
        if other_formats:
            raise ExcelToolError(
                "检测到文件「%s」格式不支持，本工具仅支持 .xlsx 格式。\n"
                "请先在 Excel 中打开该文件，点击「文件 → 另存为 → Excel 工作簿(.xlsx)」保存后，再放回清单路径重试。"
                % "、".join(other_formats)
            )
        raise ExcelToolError(
            "清单路径下未找到 .xlsx 投标机会清单（路径：%s）。\n请将政府采购云平台导出的清单文件放入该文件夹后重试。"
            % list_dir
        )

    frames, sources, header_rows, errors = [], {}, {}, []
    for fname in files:
        path = os.path.join(list_dir, fname)
        header_row = _detect_header_row(path)
        try:
            df = pd.read_excel(path, dtype=str)
        except Exception as e:
            errors.append("读取「%s」失败：%s" % (fname, e))
            continue
        if df.empty:
            errors.append("「%s」没有数据行，已跳过。" % fname)
            continue
        # 多 Sheet 主文件（不同地区不同 Sheet）：逐 Sheet 读取
        if len(pd.ExcelFile(path).sheet_names) > 1:
            try:
                sheet_frames = _load_multi_sheet(path, fname, errors)
            except Exception as e:
                errors.append("读取「%s」多 Sheet 失败：%s" % (fname, e))
                sheet_frames = []
            for sf in sheet_frames:
                frames.append(sf["df"])
                sources[sf["source"]] = path
                header_rows[sf["source"]] = sf["header_row"]
            continue
        missing = [c for c in REQUIRED_COLUMNS if c not in df.columns]
        if missing:
            errors.append("「%s」缺少必需列：%s（请确认含公告时间、公告类型、采购项目名称等列）。" % (fname, "、".join(missing)))
            continue
        df["来源文件"] = fname
        df["原始行号"] = [header_row + 1 + i for i in range(len(df))]
        frames.append(df)
        sources[fname] = path
        header_rows[fname] = header_row

    if not frames:
        raise ExcelToolError("没有可处理的投标机会清单文件。\n" + "\n".join(errors))

    merged = pd.concat(frames, ignore_index=True) if len(frames) > 1 else frames[0]
    return {"df": merged, "sources": sources, "header_rows": header_rows, "errors": errors}


def _load_multi_sheet(path: str, fname: str, errors: list) -> list:
    """
    逐 Sheet 读取多 Sheet 主文件（政府采购公告.xlsx 等，不同地区不同 Sheet）。
    返回 [{"df": DataFrame, "source": "文件名::Sheet名", "header_row": 行号}]；
    单个 Sheet 缺必需列或空时记入 errors 并跳过（不阻塞其他 Sheet）。
    """
    import pandas as pd
    out = []
    try:
        xls = pd.ExcelFile(path)
        sheet_names = xls.sheet_names
    except Exception as e:
        errors.append("读取「%s」工作表列表失败：%s" % (fname, e))
        return out
    for sn in sheet_names:
        try:
            head = pd.read_excel(path, sheet_name=sn, header=None, nrows=5, dtype=str)
        except Exception:
            continue
        header_row = 1
        for i in range(len(head)):
            row_vals = [str(v) for v in head.iloc[i].tolist() if str(v) and str(v) != "nan"]
            if any("采购项目名称" in v for v in row_vals):
                header_row = i + 1
                break
        try:
            df = pd.read_excel(path, sheet_name=sn, header=header_row - 1, dtype=str)
        except Exception as e:
            errors.append("读取「%s」Sheet「%s」失败：%s" % (fname, sn, e))
            continue
        if df.empty:
            errors.append("「%s」Sheet「%s」没有数据行，已跳过。" % (fname, sn))
            continue
        missing = [c for c in REQUIRED_COLUMNS if c not in df.columns]
        if missing:
            errors.append(
                "「%s」Sheet「%s」缺少必需列：%s（请确认含公告时间、公告类型、采购项目名称等列）。"
                % (fname, sn, "、".join(missing))
            )
            continue
        source = "%s::%s" % (fname, sn)
        df["来源文件"] = source
        df["原始行号"] = [header_row + 1 + i for i in range(len(df))]
        out.append({"df": df, "source": source, "header_row": header_row})
    return out


def _parse_types_file(path: str, errors: list = None) -> list:
    """解析项目类型选择文件，返回选中的大类范围列表 [{"名称": 大类, "子类": [名称...] 或 None(全部)}]。

    兼容三种格式：
    - 新格式：{"scope": [{"大类": "...", "子类": [...]}]} 或 [{"大类": "...", "子类": [...]}]；
    - 旧格式：{"types": ["大类名"...]} / 纯名称列表 / {"selected": [...]} —— 视为所选大类全部子类；
      旧格式名称自动映射到新 20 大类（见 OLD_TYPE_NAME_MAP），无法映射的名称（如"其他"）
      记入 errors 中文警告（不静默丢弃）；新格式（scope 列表）行为不变。
    """
    try:
        with open(path, encoding="utf-8-sig") as f:
            data = json.load(f)
    except Exception:
        return []
    template_names = {t["名称"] for t in PROJECT_TYPES_TEMPLATE}

    def resolve(name: str):
        """旧格式名称 → 新大类名称；已在新模板中则原样返回；无法识别返回 None。"""
        if name in template_names:
            return name
        return OLD_TYPE_NAME_MAP.get(name)

    def warn_unknown(names: list) -> None:
        unknown = sorted({str(n) for n in names if str(n).strip() and resolve(str(n).strip()) is None})
        if unknown and errors is not None:
            for n in unknown:
                errors.append(
                    "旧配置（%s）中存在无法识别的类型：%s，请重新选择项目类型"
                    "（建议先在对话中重新确认项目类型，本次初筛将跳过该类型）。"
                    % (PROJECT_TYPES_SELECTION_FILE, n)
                )

    entries = []
    if isinstance(data, list):
        entries = data
    elif isinstance(data, dict):
        if isinstance(data.get("scope"), list):
            entries = data["scope"]
        elif isinstance(data.get("types"), list):
            names = [str(t) if isinstance(t, str) else str(t.get("名称", "")) for t in data["types"] if t]
            warn_unknown(names)
            return [{"名称": n, "子类": None} for n in (resolve(x) for x in names) if n]
        elif isinstance(data.get("selected"), list):
            names = [str(n) for n in data["selected"] if str(n).strip()]
            warn_unknown(names)
            return [{"名称": n, "子类": None} for n in (resolve(x) for x in names) if n]
    scope = []
    for e in entries:
        if isinstance(e, str):
            # 纯名称列表为旧格式：旧名称映射到新大类，无法识别记中文警告
            n = resolve(e)
            if n:
                scope.append({"名称": n, "子类": None})
            else:
                warn_unknown([e])
        elif isinstance(e, dict):
            big = str(e.get("大类") or e.get("名称") or "").strip()
            subs = e.get("子类")
            if big:
                if isinstance(subs, list):
                    sub_names = [str(s) for s in subs if str(s).strip()]
                    scope.append({"名称": big, "子类": sub_names or None})
                else:
                    scope.append({"名称": big, "子类": None})
    return scope


def _all_subclass_names(big_name: str) -> list:
    """返回大类下全部子类名称（按模板顺序）。"""
    for t in PROJECT_TYPES_TEMPLATE:
        if t["名称"] == big_name:
            return [s["名称"] for s in t.get("子类", [])]
    return []


def load_project_types(path: str = None, errors: list = None) -> list:
    """
    读取用户本次确认的项目类型范围（清单路径/项目类型选择.json），仅返回启用的大类。

    选择文件记录用户选中的大类及子类（P3：模板固定在内置常量，不随业务路径）：
    [{"大类": "医疗健康", "子类": ["影像检查设备"]}]，子类缺省/空数组 = 该大类全部子类。
    返回 [{"名称": 大类, "子类": [名称...] 或 None(全部)}]；
    配置不存在、格式错误或无启用类型时返回 []（初筛将跳过类型过滤）。

    P3.1：旧格式名称（{"types": [...]}/纯名称列表/{"selected": [...]}）自动映射到新 20 大类
    （见 OLD_TYPE_NAME_MAP）；无法映射的名称（如"其他"）不静默丢弃，记入 errors（中文警告，
    含解决建议），由调用方输出给用户。
    """
    if not path or not os.path.exists(path):
        return []
    scope = _parse_types_file(path, errors=errors)
    template_names = {t["名称"] for t in PROJECT_TYPES_TEMPLATE}
    enabled = []
    for item in scope:
        if item["名称"] not in template_names:
            continue
        subs = item.get("子类")
        if subs:
            valid = [s for s in subs if s in set(_all_subclass_names(item["名称"]))]
            # 同类重复出现时合并子类（旧格式多旧名映射到同一大类时保证只生效一次）
            if valid:
                for prev in enabled:
                    if prev["名称"] == item["名称"]:
                        prev["子类"] = sorted(set(prev.get("子类") or []) | set(valid))
                        break
                else:
                    enabled.append({"名称": item["名称"], "子类": valid})
                continue
            subs = None
        for prev in enabled:
            if prev["名称"] == item["名称"]:
                break
        else:
            enabled.append({"名称": item["名称"], "子类": subs})
    return enabled


def list_all_project_types(path: str = None) -> list:
    """
    列出全部项目类型（大类+子类），供对话展示给用户。

    path 为清单路径/项目类型选择.json：存在时返回模板并标记其中已选大类启用状态；
    不存在时返回内置模板（全部未启用）。返回 [{"名称", "子类": [...], "启用"}]。
    """
    enabled_bigs = {item["名称"] for item in load_project_types(path)} if path else set()
    return [
        {"名称": t["名称"], "子类": [s["名称"] for s in t.get("子类", [])], "启用": t["名称"] in enabled_bigs}
        for t in PROJECT_TYPES_TEMPLATE
    ]


def save_selected_types(scope, path: str = None) -> str:
    """
    将用户本次选择的项目类型范围写入「清单路径/项目类型选择.json」（P3 起）。

    支持两种入参：
    - 二级结构（推荐）：scope = {"教育培训与设备": ["教育信息化"], "医疗健康": []}，
      值为空数组或 None 表示该大类全部子类；
    - 兼容旧格式：scope = ["教育培训与设备", "医疗健康"]（仅大类，全部子类）。

    文件内容：{"scope": [{"大类": "...", "子类": [...]}]}，可直接用记事本增删。
    """
    if not path:
        raise ExcelToolError(
            "无法保存项目类型选择：未提供输出路径。\n"
            "请传入清单路径下的文件路径，如 os.path.join(清单路径, '项目类型选择.json')。"
        )
    template_names = [t["名称"] for t in PROJECT_TYPES_TEMPLATE]
    scope_list = []
    if isinstance(scope, dict):
        for big in template_names:
            if big in scope:
                subs = scope[big]
                if isinstance(subs, list) and subs:
                    scope_list.append({"大类": big, "子类": [str(s) for s in subs]})
                else:
                    scope_list.append({"大类": big, "子类": []})
    elif isinstance(scope, (list, tuple)):
        for big in template_names:
            if big in set(str(s) for s in scope):
                scope_list.append({"大类": big, "子类": []})
    cfg = {
        "说明": "本次研判选择的项目类型范围（大类+子类，空数组=该大类全部子类；由 AI 在对话中确认后写入，每次研判可不同；可直接用记事本增删）。",
        "scope": scope_list,
    }
    os.makedirs(os.path.dirname(path) or ".", exist_ok=True)
    try:
        with open(path, "w", encoding="utf-8") as f:
            json.dump(cfg, f, ensure_ascii=False, indent=2)
    except Exception as e:
        raise ExcelToolError("保存项目类型选择文件失败（%s）：%s" % (path, e))
    return path


def save_time_range(days, path: str = None) -> str:
    """
    将用户本次选择的时间范围写入「清单路径/时间范围选择.json」。

    days 为天数（如 5/7/15/30/90）；传 None/0/"不限"/"全部" 表示不限制公告时间（不做强制限定）。
    文件内容：{"days": 5} 或 {"days": null}（null = 不限）。
    """
    if not path:
        raise ExcelToolError(
            "无法保存时间范围选择：未提供输出路径。\n"
            "请传入清单路径下的文件路径，如 os.path.join(清单路径, '时间范围选择.json')。"
        )
    if days in (None, 0, "不限", "全部", "不限制"):
        value = None
    else:
        try:
            value = int(days)
        except (TypeError, ValueError):
            value = None
    cfg = {
        "说明": "本次研判选择的公告时间范围（天）；null 表示不限制公告时间。由 AI 在对话中确认后写入，每次研判可不同；可直接用记事本修改。",
        "days": value,
    }
    os.makedirs(os.path.dirname(path) or ".", exist_ok=True)
    try:
        with open(path, "w", encoding="utf-8") as f:
            json.dump(cfg, f, ensure_ascii=False, indent=2)
    except Exception as e:
        raise ExcelToolError("保存时间范围选择文件失败（%s）：%s" % (path, e))
    return path


def load_time_range(path: str = None):
    """读取时间范围选择文件，返回天数（int）；未配置/格式错误/不限 时返回 None（不限时间）。"""
    if not path or not os.path.exists(path):
        return None
    try:
        with open(path, encoding="utf-8-sig") as f:
            data = json.load(f)
    except Exception:
        return None
    days = data.get("days") if isinstance(data, dict) else None
    if days in (None, 0, "", "不限", "全部", "不限制"):
        return None
    try:
        return int(days)
    except (TypeError, ValueError):
        return None


def _has_filled_content(company_text: str) -> bool:
    """判断公司介绍是否已填写实际内容：任意一行冒号后有非空内容视为已填写。"""
    if not company_text:
        return False
    for line in company_text.splitlines():
        if "：" in line:
            _, _, tail = line.partition("：")
        elif ":" in line:
            _, _, tail = line.partition(":")
        else:
            continue
        if tail.strip():
            return True
    return False


def extract_company_keywords(company_text: str) -> list:
    """从公司介绍文本中抽取业务/产品相关关键词（2 字及以上中文词），用于相关性初判。"""
    if not company_text:
        return []
    # 剔除模板引导说明行（> 开头的 blockquote），避免空模板被误判为公司业务内容
    lines = [ln for ln in company_text.splitlines() if not ln.lstrip().startswith(">")]
    company_text = "\n".join(lines)
    # 去除非中文字符和常见停用词
    text = re.sub(r"[^\u4e00-\u9fa5a-zA-Z0-9]+", " ", company_text)
    stopwords = {
        "我们", "公司", "有限公司", "本司", "企业", "业务", "产品", "服务",
        "以及", "以及的", "以及公司", "主要", "提供", "包括", "拥有", "具备",
        "相关", "各项", "以及本", "以及主", "以下", "如下", "相应", "领域",
    }
    words = set()
    # 中文双字词
    for m in re.findall(r"[\u4e00-\u9fa5]{2,8}", text):
        w = m.strip()
        if w and w not in stopwords and len(w) >= 2:
            words.add(w)
    # 英文/数字词
    for m in re.findall(r"[a-zA-Z0-9]{2,}", text):
        words.add(m.lower())
    return sorted(words)


def _stringify(value) -> str:
    if value is None:
        return ""
    text = str(value).strip()
    if text.lower() in ("nan", "none", "null"):
        return ""
    return text


def classify_project(name: str, buyer: str = "", announce_type: str = "") -> dict:
    """
    按 20 大类 × 二级子类关键词体系对单个项目/业绩名称分类（初筛与业绩表共用）。
    五级匹配保证 100% 命中、无"未分类"兜底：
      1) 子类关键词（项目名命中子类关键词，最精准）
      2) 电子卖场公告规则（公告类型=电子卖场公告 → 电子卖场类）
      3) 大类语义词（BIG_SEMANTIC_WORDS，项目名命中大类宽词）
      4) 采购人行业域（SECTOR_BUYER_KWS，采购人属某行业且项目名含行业词）
      5) 标题形态兜底（FORM_FALLBACK，项目名含"工程/服务/系统/设备"等形态词 → 归入对应大类）

    返回 {"pairs": [(大类, 子类)...], "display": "大类（子类）…", "pending": bool}
    """
    proj = _stringify(name)
    buyer = _stringify(buyer)
    atype = _stringify(announce_type)
    matched_pairs = []
    hit_kws = {}
    # 1) 子类关键词
    for big in PROJECT_TYPES_TEMPLATE:
        for sub in big.get("子类", []):
            kws = [k for k in sub["关键词"] if k in proj]
            if kws:
                matched_pairs.append((big["名称"], sub["名称"]))
                hit_kws[(big["名称"], sub["名称"])] = kws
    # 2) 电子卖场公告 → 电子卖场类（公告类型为"电子卖场公告"即归入，不受项目名限制）
    if atype == "电子卖场公告" and not any(b == "电子卖场类" for b, _ in matched_pairs):
        matched_pairs.append(("电子卖场类", "电子卖场类"))
    if not matched_pairs:
        # 3) 采购人行业域（采购人关键词 → 大类）
        for big, buyer_kws in SECTOR_BUYER_KWS:
            if any(k in buyer for k in buyer_kws):
                matched_pairs.append((big, big))
                break
    if not matched_pairs:
        # 4) 大类语义词（项目名命中大类宽词）
        for big, kws in BIG_SEMANTIC_WORDS.items():
            if any(k in proj for k in kws):
                matched_pairs.append((big, big))
                break
    if not matched_pairs:
        # 5) 标题形态兜底（通用词 → 大类，保证 100% 命中）
        for kws, big in FORM_FALLBACK:
            if any(k in proj for k in kws):
                matched_pairs.append((big, big))
                break
    pending = not matched_pairs
    if pending:
        # 理论不可达（五级匹配已全覆盖）；保险起见归入专业服务但保持 pending 标记
        matched_pairs = [("专业服务", "专业服务")]
    grouped = {}
    for big, sub in matched_pairs:
        grouped.setdefault(big, []).append(sub)
    display_parts = []
    for big, subs in grouped.items():
        if len(subs) == 1 and subs[0] == big:
            display_parts.append(big)
        elif len(subs) == 1:
            display_parts.append("%s（%s）" % (big, subs[0]))
        else:
            display_parts.append("%s（%s）" % (big, "、".join(subs)))
    return {"pairs": matched_pairs, "display": "、".join(display_parts), "pending": pending, "hits": hit_kws}


def filter_screening(
    df: pd.DataFrame,
    company_text: str = "",
    days: int = None,
    today: datetime = None,
    project_types: list = None,
) -> pd.DataFrame:
    """
    初筛：时间范围（可选，不强制）+ 公告类型合法 + 项目类型命中。

    days 为 None/0 时不做时间过滤（不限公告时间，全部按"时间通过"处理）；
    否则仅保留公告时间在 days 天以内的项目。

    返回 DataFrame，新增列：
    - 项目类型: 命中的已选项目类型名称（未命中为空）
    - 初筛结果: 通过 / 时间不符 / 类型不符 / 项目类型不符 / 时间缺失
    - 初筛原因: 各过滤项的明确原因（如"公告超过 N 天""不在已选项目类型范围"），通过项为"通过"
    - 相关性初判: 相关 / 待确认（软标记，不参与过滤，仅作研判提示）
    - 相关性依据: 命中的公司关键词，或未命中的说明

    相关性为软标记：不命中也不剔除（避免因关键词覆盖不全误杀项目），
    由 AI 在对话中向用户确认后决定是否研判。

    project_types 为用户确认的启用类型列表（见 load_project_types）；
    为 None 时自动读取清单路径/项目类型选择.json；无启用类型时跳过类型过滤。
    """
    if today is None:
        today = datetime.now()
    # days 为 None/0 = 不限制时间（不做时间过滤，全部按时间通过）
    cutoff = None if not days else today - timedelta(days=days)

    if project_types is None:
        project_types = load_project_types()

    company_keywords = extract_company_keywords(company_text)
    has_company_data = _has_filled_content(company_text)

    df = df.copy()
    results = []
    rel = []
    rel_why = []
    matched_type_names = []
    type_pending_flags = []

    for _, row in df.iterrows():
        # 1) 时间条件（不限制时跳过；限制时公告时间缺失仍标记"时间缺失"）
        if cutoff is None:
            results.append("时间通过")
        else:
            dt = _parse_date(row.get("公告时间"))
            if dt is None:
                results.append("时间缺失")
            else:
                # 时间比较统一到日期
                if dt.timestamp() >= cutoff.timestamp():
                    results.append("时间通过")
                else:
                    results.append("时间不符")

        # 2) 类型条件
        atype = _stringify(row.get("公告类型"))
        if atype in VALID_ANNOUNCE_TYPES:
            type_ok = True
        else:
            type_ok = False
            if results[-1] == "时间通过":
                results[-1] = "类型不符"

        # 3) 项目类型过滤（按清单路径/项目类型选择.json 中用户本次确认的大类+子类范围）
        #    项目类型标注用全量模板（20 大类 × 子类）五级匹配（未选中的类型也标注），过滤只用已选范围
        proj = _stringify(row.get("采购项目名称"))
        buyer = _stringify(row.get("采购人"))
        atype = _stringify(row.get("公告类型"))
        cls = classify_project(proj, buyer=buyer, announce_type=atype)
        matched_pairs = cls["pairs"]
        type_pending_flag = cls["pending"]
        # 过滤：是否命中用户本次选择的大类+子类范围（子类为空数组=大类全部子类）
        if project_types and results[-1] == "时间通过" and type_ok:
            allowed = set()
            for item in project_types:
                subs = item.get("子类")
                if subs is None:
                    allowed.add((item["名称"], None))  # 大类级：命中该大类任意子类均可
                else:
                    allowed.update((item["名称"], s) for s in subs)
            hit_selected = False
            for big, sub in matched_pairs:
                if (big, None) in allowed or (big, sub) in allowed:
                    hit_selected = True
                    break
            if not hit_selected:
                # v3.2 无兜底：五级匹配保证每个项目都有明确类型，未命中即不在已选范围
                results[-1] = "项目类型不符"
        matched_type_names.append(cls["display"])
        type_pending_flags.append(type_pending_flag)

        # 4) 相关性初判（软标记：命中"相关"；未命中或资料不齐"待确认"，不剔除）
        if has_company_data and company_keywords and proj:
            hits = [k for k in company_keywords if k in proj]
            if hits:
                rel.append("相关")
                rel_why.append("命中词：" + "、".join(sorted(hits)))
            else:
                rel.append("待确认")
                rel_why.append("无公司关键词命中（关键词：" + "、".join(company_keywords[:12]) + "…）")
        else:
            rel.append("待确认")
            rel_why.append("企业资料未填写完整，暂无法初判")

    df["项目类型"] = matched_type_names
    df["相关性初判"] = rel
    df["相关性依据"] = rel_why

    # 综合判定：时间+类型+项目类型 三项同时通过才进入候选清单
    # 相关性不参与过滤，仅作软标记；v3.2 五级匹配保证每个项目都有明确类型（无未分类兜底）
    def decide(r):
        if r == "时间不符":
            return "时间不符", "公告超过 %d 天" % (days or 0)
        if r == "类型不符":
            return "类型不符", "公告类型不符"
        if r == "时间缺失":
            return "时间缺失", "公告时间缺失"
        if r == "项目类型不符":
            enabled_names = "、".join(
                (t["名称"] if t.get("子类") is None else "%s（%s）" % (t["名称"], "、".join(t["子类"])))
                for t in project_types
            ) if project_types else ""
            return "项目类型不符", "不在已选项目类型范围（%s）" % enabled_names
        return "通过", "通过"

    df["初筛结果"] = [decide(r)[0] for r in results]
    df["初筛原因"] = [decide(r)[1] for r in results]
    df["类型待确认"] = type_pending_flags
    return df


def filter_passed(df: pd.DataFrame) -> pd.DataFrame:
    """仅保留初筛通过（时间+类型+项目类型均合法）的候选。"""
    return df[df["初筛结果"] == "通过"].copy()


# 初筛结果 Excel 输出列（P3：{时间}_初筛结果.xlsx，各清单一个 sheet）
SCREENING_EXCEL_COLUMNS = [
    "项目类型",
    "公告时间",
    "公告类型",
    "所在区县",
    "采购人",
    "采购项目名称",
    "预算金额(元)",
    "中标金额(元)",
    "中标单位",
    "代理机构",
    "地址链接URL",
    "相关性初判",
    "相关性依据",
    "初筛结果",
    "初筛原因",
    "来源文件",
    "原始行号",
]
# 输出列显示名：相关性依据 → 相关性(命中词)（验收口径：含初筛结果/初筛原因/相关性(命中词)/来源列）
SCREENING_EXCEL_DISPLAY = {"相关性依据": "相关性(命中词)"}


def _safe_sheet_title(name: str) -> str:
    """工作表名：去扩展名 + 去非法字符 + 截断 31 字符（Excel 限制）。"""
    title = os.path.splitext(os.path.basename(name))[0]
    title = re.sub(r"[\\/:*?\[\]]", "_", title)
    title = title[:31] or "清单"
    return title


def write_screening_excel(
    df: pd.DataFrame,
    list_dir: str,
    sources: dict,
    timestamp: str,
    output_path: str = None,
    extra_columns: list = None,
) -> str:
    """
    输出初筛结果 Excel（P3）：{时间}_初筛结果.xlsx，放在清单路径下。

    - 每个来源清单一个 sheet（表名取清单文件名去扩展名），含全量行；
    - 每行含：初筛结果 / 初筛原因 / 相关性初判 / 相关性依据(命中词) / 来源文件 / 原始行号；
    - extra_columns（可选，默认 None）：追加到输出列末尾的列名列表
      （增量模式下传 ["初筛状态", "研判状态"]；不传时行为与既有完全一致）；
    - 返回输出文件绝对路径；保存失败时抛出中文提示（含被 Excel 占用的处理建议）。
    """
    if output_path is None:
        output_path = os.path.join(list_dir, "%s_初筛结果.xlsx" % timestamp)
    os.makedirs(os.path.dirname(output_path) or ".", exist_ok=True)

    from openpyxl import Workbook

    try:
        wb = Workbook()
        wb.remove(wb.active)
        used_titles = set()
        # 汇总 sheet（置顶）：仅保留初筛通过且相关性为"待确认"的项目，供用户集中确认
        pending = df[(df["初筛结果"] == "通过") & (df["相关性初判"] == "待确认")]
        if len(pending):
            _write_sheet(wb, "汇总", pending, extra_columns)
            used_titles.add("汇总")
        # 无来源信息的行归入"合并清单"sheet
        source_names = [n for n in sources.keys()]
        if source_names:
            for fname in source_names:
                sub = df[df["来源文件"] == fname]
                title = _safe_sheet_title(fname)
                while title in used_titles:
                    title = title + "_2"
                used_titles.add(title)
                _write_sheet(wb, title, sub, extra_columns)
            # 来源文件不在 sources 中的行（理论不会出现，兜底）
            rest = df[~df["来源文件"].isin(source_names)]
            if len(rest):
                _write_sheet(wb, "合并清单", rest, extra_columns)
        else:
            _write_sheet(wb, "清单", df, extra_columns)
        wb.save(output_path)
    except PermissionError:
        raise ExcelToolError(
            "保存初筛结果失败：%s 可能已被 Excel 打开占用，请关闭 Excel 后重试。" % output_path
        )
    except Exception as e:
        raise ExcelToolError("保存初筛结果失败（%s）：%s" % (output_path, e))
    finally:
        try:
            wb.close()
        except Exception:
            pass
    return output_path


def _write_sheet(wb, title: str, df: pd.DataFrame, extra_columns: list = None) -> None:
    """将 DataFrame 写入工作簿的单个 sheet（只写初筛结果相关列；extra_columns 追加到末尾）。"""
    ws = wb.create_sheet(title)
    cols = [c for c in SCREENING_EXCEL_COLUMNS if c in df.columns]
    for c in (extra_columns or []):
        if c in df.columns and c not in cols:
            cols.append(c)
    headers = [SCREENING_EXCEL_DISPLAY.get(c, c) for c in cols]
    ws.append(headers)
    for _, row in df.iterrows():
        ws.append([_stringify(row.get(c)) for c in cols])
    # 简单列宽（不冻结，保持轻量）
    from openpyxl.utils import get_column_letter
    for i, h in enumerate(headers, start=1):
        width = min(max(len(h) * 2 + 2, 12), 40)
        ws.column_dimensions[get_column_letter(i)].width = width


def _raise_if_locked(path: str) -> None:
    """写前检查 Excel 锁文件（~$文件名）；被 Excel 打开占用时给出中文提示并中止。"""
    directory, name = os.path.split(path)
    lock = os.path.join(directory, "~$" + name)
    if os.path.exists(lock):
        raise ExcelToolError(
            "错误：主文件被 Excel 打开，无法写入，请先关闭 Excel 再运行。\n"
            "  主文件：%s\n  锁文件：%s\n"
            "（本次结果未回写，不影响其他产物；关闭 Excel 后重跑一次初筛即可补齐）" % (path, lock)
        )


def write_screening_back_to_main(df: pd.DataFrame, sources: dict, main_file: str = None) -> bool:
    """
    初筛结果回写主文件（多 Sheet 主文件工作流：不新建初筛结果 Excel）。

    - 来源名为「文件名::Sheet名」时，按 URL 定位主文件对应 Sheet 的行，
      回写「初筛结果 / 初筛原因 / 相关性(命中词)」三列；
    - 主文件参数缺省时从 sources 推断唯一文件路径；
    - 返回值：True=已回写（至少一个 Sheet 生效）；False=未回写（非多 Sheet 来源或主文件不可写）。
    """
    if not sources or df is None or df.empty:
        return False
    # 仅处理多 Sheet 来源（来源名含 ::）
    sheet_sources = {src: path for src, path in sources.items() if "::" in str(src)}
    if not sheet_sources:
        return False

    # 推断主文件：所有 Sheet 来源指向同一文件（如 政府采购公告.xlsx）
    paths = set(sheet_sources.values())
    if len(paths) != 1:
        return False
    main_path = paths.pop()
    if main_file and os.path.normpath(main_file) != os.path.normpath(main_path):
        main_path = main_file

    # 写前检查锁文件（与 status_tool 口径一致），避免主文件被 Excel 占用时静默失败
    _raise_if_locked(main_path)

    from openpyxl import load_workbook

    try:
        wb = load_workbook(main_path)
    except Exception as e:
        raise ExcelToolError("打开主文件「%s」失败，请确认文件未被 Excel 占用。详细信息：%s" % (main_path, e))

    # 按来源分组：来源名 → 表头行号（探测）
    src_header = {}
    for src in sheet_sources:
        fname, _, sn = src.partition("::")
        if sn not in wb.sheetnames:
            continue
        ws = wb[sn]
        header_row = None
        for r in range(1, min(ws.max_row, 5) + 1):
            vals = [str(ws.cell(r, c).value or "").strip() for c in range(1, ws.max_column + 1)]
            if "采购项目名称" in vals:
                header_row = r
                break
        if header_row is None:
            continue
        src_header[src] = (ws, header_row)

    if not src_header:
        wb.close()
        return False

    # 构建 (Sheet名, URL) → (Sheet, 列名→列号, 行号) 映射。
    # 键必须含 Sheet 名：同一 URL 可能出现在不同地市 Sheet（市本级/区县重复挂载），
    # 仅按 URL 建映射会被后遍历的 Sheet 覆盖，导致回写错位。
    url_to_loc = {}
    for src, (ws, header_row) in src_header.items():
        col_of = {}
        for c in range(1, ws.max_column + 1):
            name = str(ws.cell(header_row, c).value or "").strip()
            if name:
                col_of.setdefault(name, c)
        url_col = col_of.get("地址链接URL")
        if not url_col:
            continue
        sheet_name = src.partition("::")[2]
        for r in range(header_row + 1, ws.max_row + 1):
            url = str(ws.cell(r, url_col).value or "").strip()
            if url:
                url_to_loc[(sheet_name, url)] = (ws, col_of, r)

    written = 0
    for _, row in df.iterrows():
        url = _stringify(row.get("地址链接URL")).strip()
        if not url:
            continue
        src = _stringify(row.get("来源文件"))
        sheet_name = src.partition("::")[2] if "::" in src else ""
        loc = url_to_loc.get((sheet_name, url))
        if loc is None:
            continue
        ws, col_of, r = loc
        fields = {
            "初筛结果": row.get("初筛结果"),
            "初筛原因": row.get("初筛原因"),
            "相关性(命中词)": row.get("相关性依据"),
        }
        for col_name, val in fields.items():
            c = col_of.get(col_name)
            if c is not None:
                ws.cell(r, c).value = _stringify(val) if val is not None else ""
        written += 1

    if written:
        # 保存前再次检查锁：逐行写入耗时较长，期间文件可能被 Excel 打开（锁在写入中才出现）
        _raise_if_locked(main_path)
        try:
            wb.save(main_path)
        except PermissionError:
            wb.close()
            raise ExcelToolError(
                "保存主文件失败：%s 可能已被 Excel 打开占用（或被杀毒软件/云同步临时锁定），"
                "请先关闭 Excel 再运行；若已关闭仍报此错，请稍等几秒后重试。" % main_path
            )
    wb.close()
    return written > 0


def build_screening_md_table(df: pd.DataFrame) -> str:
    """
    生成初筛汇总 markdown 表格（对话中初筛汇报用；研判报告已不含初筛汇总，仅逐项目研判）。

    列：项目类型 | 公告时间 | 公告类型 | 采购项目名称 | 相关性(命中词) | 初筛结果 | 初筛原因 | 来源
    返回 markdown 表格文本；df 为空时返回"（无初筛数据）"。
    """
    if df is None or df.empty:
        return "（无初筛数据）"
    header = ["项目类型", "公告时间", "公告类型", "采购项目名称", "相关性(命中词)", "初筛结果", "初筛原因", "来源"]
    lines = ["| " + " | ".join(header) + " |", "|" + "---|" * len(header)]
    for _, row in df.iterrows():
        cells = [
            _stringify(row.get("项目类型")),
            _stringify(row.get("公告时间")),
            _stringify(row.get("公告类型")),
            _stringify(row.get("采购项目名称")),
            _stringify(row.get("相关性依据")),
            _stringify(row.get("初筛结果")),
            _stringify(row.get("初筛原因")),
            "%s / 行%s" % (_stringify(row.get("来源文件")), _stringify(row.get("原始行号"))),
        ]
        cells = [c.replace("|", "｜").replace("\n", " ") for c in cells]
        lines.append("| " + " | ".join(cells) + " |")
    return "\n".join(lines)


def write_result(
    source_paths,
    output_path: str,
    result_map: dict,
    extra_columns: list = None,
) -> None:
    """
    将研判结果回写到新 Excel：每个来源清单一个工作表，保留原表结构与格式，
    新增「研判结果」列（及 extra_columns 指定的列，如"项目类型"）。

    【P3 说明】本函数自 P3 起不再被 SKILL.md 调用（研判结论改输出 md 报告），
    仅保留以兼容历史脚本。

    参数：
    - source_paths: 来源清单 {文件名: 绝对路径}；兼容传单个字符串路径（自动包一层）
    - output_path: 输出文件路径
    - result_map: {(来源文件名, Excel行号): {"项目类型": str, "研判结果": str}}
      兼容旧格式：单文件时 key 可为 {Excel行号: 文本}，内部自动转换
    - extra_columns: 在「研判结果」之前插入的列头列表（默认 ["项目类型"]）
    """
    if isinstance(source_paths, str):
        source_paths = {os.path.basename(source_paths): source_paths}

    if not source_paths:
        raise ExcelToolError("无法回写：未提供任何来源清单文件。")

    extra_columns = extra_columns or ["项目类型"]
    from openpyxl import load_workbook

    # 规范化 result_map：key -> (来源文件名, 行号)
    norm = {}
    for key, val in result_map.items():
        if isinstance(key, tuple) and len(key) == 2:
            fname, row_idx = key
        else:
            fname, row_idx = list(source_paths.keys())[0], key
        if isinstance(val, str):
            val = {"研判结果": val}
        norm[(fname, int(row_idx))] = val

    os.makedirs(os.path.dirname(output_path) or ".", exist_ok=True)
    out = None  # 输出工作簿

    for fname, path in source_paths.items():
        if not os.path.exists(path):
            raise ExcelToolError("无法回写：源文件不存在：%s" % path)
        try:
            wb = load_workbook(path)
        except Exception as e:
            raise ExcelToolError("打开源文件「%s」失败，请确认文件未被占用。详细信息：%s" % (fname, e))

        ws = wb.active
        # 找到表头所在行
        header_row = None
        for r in range(1, min(ws.max_row, 5) + 1):
            vals = [str(ws.cell(row=r, column=c).value or "") for c in range(1, ws.max_column + 1)]
            if any("采购项目名称" in v for v in vals):
                header_row = r
                break
        if header_row is None:
            header_row = 1

        # 在「研判结果」前插入 extra_columns（从最后一列之后开始写）
        new_col = ws.max_column + 1
        col_of = {}
        for col_name in list(extra_columns) + ["研判结果"]:
            ws.cell(row=header_row, column=new_col, value=col_name)
            if header_row > 1:
                src_cell = ws.cell(row=header_row, column=1)
                new_cell = ws.cell(row=header_row, column=new_col)
                try:
                    new_cell.font = src_cell.font.copy()
                    new_cell.fill = src_cell.fill.copy()
                    new_cell.border = src_cell.border.copy()
                    new_cell.alignment = src_cell.alignment.copy()
                except Exception:
                    pass
            col_of[col_name] = new_col
            new_col += 1

        # 回写结果（行号从表头+1 开始）
        for (src_fname, row_idx), vals in norm.items():
            if src_fname != fname:
                continue
            for col_name, value in vals.items():
                if col_name in col_of:
                    ws.cell(row=row_idx, column=col_of[col_name], value=value)

        sheet_title = fname.rsplit(".", 1)[0][:31]

        if out is None:
            out = wb
            try:
                out.active.title = sheet_title
            except Exception:
                pass
        else:
            import copy as _copy
            try:
                out.create_sheet(sheet_title)
            except Exception:
                out.create_sheet(sheet_title + "_" + str(len(out.sheetnames)))
            dst = out[out.sheetnames[-1]]
            for row in ws.iter_rows():
                for cell in row:
                    nc = dst[cell.coordinate]
                    nc.value = cell.value
                    nc.font = _copy.copy(cell.font)
                    nc.fill = _copy.copy(cell.fill)
                    nc.border = _copy.copy(cell.border)
                    nc.alignment = _copy.copy(cell.alignment)
                    nc.number_format = cell.number_format
            for rng in ws.merged_cells.ranges:
                dst.merge_cells(str(rng))
            for col_letter, dim in ws.column_dimensions.items():
                if dim.width:
                    dst.column_dimensions[col_letter].width = dim.width
            wb.close()

    try:
        out.save(output_path)
    except PermissionError:
        raise ExcelToolError(
            "保存结果文件失败：%s 可能已被 Excel 打开占用，请关闭 Excel 后重试。" % output_path
        )
    except Exception as e:
        raise ExcelToolError("保存结果文件失败：%s" % e)
    finally:
        if out is not None:
            try:
                out.close()
            except Exception:
                pass


def build_result_map(df: pd.DataFrame, results: list) -> dict:
    """
    将研判结果（按行顺序）绑回原表行号。

    参数：
    - df: 带原始 Excel 行号列（如含"原始行号"）的 DataFrame
    - results: 与 df 行顺序一致的研判结果列表
    返回 {Excel行号: 结果文本}
    """
    if len(results) != len(df):
        raise ExcelToolError("研判结果数量与候选数量不一致，无法回写。")
    if "原始行号" not in df.columns:
        raise ExcelToolError("缺少原始行号列，无法正确回写。")
    return {int(rid): text for rid, text in zip(df["原始行号"], results)}